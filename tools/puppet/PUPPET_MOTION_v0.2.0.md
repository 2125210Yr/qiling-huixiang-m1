# Puppet 次级运动 v0.2.0

已接入现有 PuppetRig：
- 左右长发区域分别受根部/末端两组阻尼状态驱动，运动在躯干、握柄及剑刃附近衰减。当前仍是同一张画布上的局部网格变形，不是假称完整拆层。
- 左右胸部单独限位弹簧；点击增加速度而不重置位置。参数用于风格化弹性表现，未做生物力学校准。
- 点击面部产生轻微头部方向响应；自动/触发眨眼仅替换原画眼部局部，鼻子嘴巴脸型继续使用原图。
- motion 模型使用 120Hz 固定时间步和解析衰减弹簧；异常时间与应用恢复有保护。

未完成：
- 挥剑只完成蓄势/挥出/回位的数值轨迹测试，没有接入实际肩臂和剑。现有剑 alpha 缺少握柄，现有身体层没有可靠遮挡补画。
- 完整独立发束、眼球视线、口型、较大转头仍需要合格拆层；当前头部响应不等于眼球转动。
- 旧单图与资源全部保留。身体补画候选未采用，因为脸/轮廓漂移、手臂没有正确移除，且输出 RGB 烘焙了棋盘背景。

image_gen：
- 使用内置工具，从原图面部裁切 (465,230)-(615,340) 的参考生成闭眼局部。
- 眼部生成提示：仅将双眼改为自然闭眼，保持身份、脸角度、头发、眉鼻唇、肤色、光照和裁切位置，不重新设计。完整提示见本文件同目录生成记录。
- 最终项目素材：client/Assets/Resources/Art/Characters/C001/PuppetMotion/blink.png。着色器只采样两只眼睛的局部，不覆盖全脸。

验证：
- tools/puppet/test-motion.ps1：11 项通过，覆盖不同帧率、弹簧衰减、左右独立输入、重复限位、技能回位、眨眼与异常时间。
- tools/puppet/test-rig.ps1：原有 10 项通过。
- Unity 6000.3.23f1：实际渲染并采样 240 个姿态，无翻折；最小三角形面积比 0.8856172。
- 视频来自 Unity 实际渲染帧，只是本次功能预览，不代表挥剑已经完成；独立 EXE 仍需重新构建。
