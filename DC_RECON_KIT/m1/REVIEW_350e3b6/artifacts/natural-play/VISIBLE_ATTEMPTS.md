# 可见窗口录制尝试记录

用户于 2026-09-20 明确允许显示 Unity 窗口。所有目录原件保留，不将失败尝试改写为通过。

| 归档前缀 | 情况 |
|---|---|
| `20260920T062020` | 默认 GI Cache 报错；脚本未取得结果，未验收。 |
| `20260920T062123` | Editor 日志有场景 PASS，但旧脚本读已清理的 Temp 导致归档失败；标题窗口捕获的 8 秒帧全白。 |
| `20260920T062640` | basic 场景、真实游戏视频及首场读回通过；交付 `gameplay-proof.mp4`，截取信息在同目录 `proof.json`。 |
| `20260920T062859` | 会话中断，尚未启动录制；核查原进程不存在后恢复备份，并单文件移除其残留 EditorInstance.json。 |
| `20260920T063947` / `20260920T064208` | 严格前台句柄检查在录制前拒绝；没有伪造录像或结果。 |
| `20260920T064419` | Fever 场景 PASS，但首次读回缺少终局后的 `fever_end/TimeUp`；原片后段被 Chrome 遮挡，不作为 Fever 完整视频验收。保留失败读回 `visible-fever.txt`。 |
| `20260920T064643` | Auto 场景及首场读回通过；视频派生片段的独立检查在同目录 `proof.json`。 |
| `20260920T071823` | 置顶窗口补录 Fever，场景 PASS、首场 1141/1141 事件精确读回通过；视频片段验收见同目录 `proof.json`。 |

`basic-focus.mp4` 是历史脚本固定使用的原始录屏名称，三个场景都沿用；不能根据这个文件名判断场景。实际场景以 `run.json`、`natural-play.result.txt` 和 battle 文件为准。

原始 desktop 录屏可能含启动前、被遮挡时或退出后的其他应用；本轮新增 raw 视频仅留本机，不加入 Git。交付片段精确按 PTS 裁剪，保留原文件 SHA、原时间范围及完整解码和视觉抽样结果，不覆盖原片。PNG 抽样也仅留本机。

脚本的 `Scenario=PASS` 仅表示 Unity 场景完成；`videoStatus=REQUIRES_VISUAL_VERIFICATION` 和前台警告不是视频通过证明。可见录像仍需独立核验。后来为避免其他应用遮挡，在本次专属 Unity 进程存活期间将窗口置顶，进程退出后结束。
