# G2 路 A 有限收口与独立试玩交付

2026-09-20，依据用户指定的《查找天命之子代码》6pro 意见（会话 `6aaf9d4f-1f40-83ea-a86c-1a55ff9bbcd6`）。开始时主仓 HEAD 为 `38629718`，远端 `m1-gt6-review` 实查仍为 `350e3b6459081807cd3699a530c83dbf40b66886`。没有推送授权，不推送。

## 本轮验收范围

1. 定点核对 Fever 终局收尾：预期状态和事件只比较，不驱动模拟；独立记录的结束时钟可以重放。篡改预期不能改变重演结果；终局没有额外伤害、战斗结算或重复奖励。
2. 准确说明既有 NEXT 第二场 8 条事件的范围。其 tick 8、Outcome=InProgress，唯一命令为 Pause；8 个事件均为 tick 0 的进入波次/状态施加。证明开局属性和这段已录片段，不证明第二场打完。
3. 复用已有源码/构建/测试/回放证据，补齐当前身份说明。唯一旧跳过为 `N01_N02_N03_NaturalPlayContract_DocumentedNotFaked`，核心 xUnit 不能替代 Unity 指针操作。事后核对不得冒充事前冻结。
4. 使用最终独立 Win64 程序，经普通首页 → 关卡 → 正常玩家输入 → 结算 → NEXT → 返回。记录第二场实际覆盖范围。禁止战斗后注入 HP、充能、Drive、时间、胜负或强制 Perfect。
5. 交付可独立启动的 Windows 试玩包和该版本实际操作录像；只交付检查过的游戏区域视频，原始桌面录制不上传。保留历史证据及其版本限制。
6. 并列报告 `PATH_A_ENGINEERING`、`M1_FIDELITY=DEFERRED_NOT_REMOVED`、`G3=NOT_STARTED`、`REMOTE_SYNC`。仅列最多 3 项已有 P0/P1 支持的后续体验候选，不自动实现。

## 边界与当前发现

此前 D1 默认开局、D2 真实点敌、Growth 舍入的修复保留。发现 `38629718` 的 `FinishRecordedFever` 以预期 `FinalDigest.FeverActive` 决定是否推进，必须最小修正；不能因旧报告为绿而跳过真实缺陷。缺独立结束边界的旧 Fever 档案原件保留，不回填、不放宽比较。

最近的仓库来件收口约定为 `../../../REVIEW_a82801b/REVIEW.md` 和同目录 `NEXT_TASK.md`；D 批目录没有另一份 REVIEW.md。只针对新发现的收尾缺陷补回归与受影响构建/运行，复用其余有效历史证据，不重新做 G0/G1、不启动 G3/M2，不扩展养成、抽卡、商城、网络或角色动画方案，也不恢复已暂停资料采集。

Windows 预检已用原独立程序普通入口到达结算。这只是启动可用性预检，正式交付将绑定边界修复后的实际构建与录像。试玩前已逐文件备份原存档；初始试玩档用未修改的 `SaveBlob` 默认值和 `EnsureStarterKit` 在启动前生成，结束后恢复原文件。
