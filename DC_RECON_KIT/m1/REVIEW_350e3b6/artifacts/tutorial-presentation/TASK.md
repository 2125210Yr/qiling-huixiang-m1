# 下一步：战斗教程提示避让技能演出

用户于 2026-09-20 明确要求“接着开发下一步”。本批从已交付候选中选择第 1 项：修正教程提示的遮挡与触发时机。起点为主仓 `92929d3f`，已有功能修复 `672cdff6`。其余两个体验候选暂不实施。

## 根因与验收

已有实际 `np_03_slide.png` 和独立试玩视频均出现教程面板覆盖技能演出。BattleHud 的延迟与就绪提示入口分别执行，缺少 SHOWTIME / QTE / 暂停 / 终局的统一展示条件；VfxTipPlate 独立计时且置顶，已存在的提示也不会避让。延迟条件以 delay > 0 作为外层门槛，到期但遇另一提示时不再重试。技能成功释放后，旧操作提示仍可能继续显示。

1. 已存在与新触发的普通提示都避让 SHOWTIME、Drive QTE、评分、暂停和终局；阻挡期间保留提示剩余寿命。
2. 计时到期遇另一提示，解除阻挡后仍能出现，不永久丢失。
3. 成功释放的 Tap / Slide / Drive 消费对应过时操作提示；敌方、自动普攻或被拒绝输入不能消费。
4. 保留 Fever 专属提示链，不以 HUD 直接修改模拟暂停、伤害、充能、QTE 时钟或回放。
5. 使用 Unity UI 行为回归与渲染检查验证实际组件，再验证 Win64 构建。测试夹具与真实普通试玩分别报告，不用核心 dotnet 测试冒充 HUD 覆盖。

使用已有 P0/P1 中明确的展示结构，不新增参考采集，不声称精确原作教程规则或原作还原验收。保持未改核心的有效历史验证，不重新扩展 G0/G1/G3/M2、角色动画或养成系统。
