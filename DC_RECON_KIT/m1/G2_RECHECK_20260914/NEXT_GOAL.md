# /goal：2a00445 复查后的 G2 收口批次

将本目录放到 DC_RECON_KIT/m1/G2_RECHECK_20260914。若已有活动 goal，把下面指令追加到该 goal；不要启动第二个修改同一工作区的主协调者。

```text
/goal 继续当前仓库 G2 / M1 路 A工程批次。先读已有项目规则、延期补件，以及 DC_RECON_KIT/m1/G2_RECHECK_20260914/REVIEW.md 和 REGRESSIONS.md。

本轮审查基线为 m1-gt6-review 的 2a00445c67dbb2ca9251d81428e3096b71c233fb。如果当前 HEAD 更新，先核对差异并记录新的修复基线，不机械重做已修复项。保护用户未提交文件，不重置、不清理无关资产，不强推。

目标：完成同一 G2 的真实操作、效果全路径、输入回放和证据保存闭环；保留已有手动Fever/种子暴击/主QTE时钟/状态实例修复，不推倒重写，不重做G0/G1，不开G3。

【最大并行】
检查当前可用执行环境，按环境允许的最大并行 subagent 数量调度，不人为设4/8/12上限，不伪造无限或已启动数量。先核对旧代理是否仍运行，为每项任务绑定 batch_id、task_id、允许路径和worktree。恢复中断会话时先接管旧任务，不重复派发同一任务。BattleSim.cs/BattleHud.cs/GameRoot.cs及活动Unity资源由唯一集成者落盘；其他代理提交独立补丁和测试。不要求为了本批新建大型调度后台。

【任务一：真实动作与验证场景】
保留 run7 FAIL 基线。不得接受“缺Tap/Slide也算通过”，不得把Fever继续从工程验收矩阵删除。
本批允许在明确标注 DESIGN_PLACEHOLDER 的工程验证配置中，于开战前设置合成阵容、有限敌方HP/攻击、时长和占位充能政策。这是原来允许的测试场景，不是新模式或原作数值定版。
先记录技能就绪、Drive满、施放与战斗结束时间线；重点检查 TickUnit 的 Math.Max(14f, autoSkill.DriveGain)，不要只修改一张表却忽略最小值仍在生效。
不要为通过测试随意改掉参考所需的Drive/Tap输入映射。用最小、显式的工程配置/硬编码修复解决可达性，提交前说明改的是哪层及适用范围。
自然操作必须走真实UI/射线/手势与Submit，开战后不得改HP、充能、Drive、Fever、时间或胜负，不得强制Perfect、不清场特效伪造过程。
可以拆为基本操作、手动Fever、自动模式三个场景，分别必须断言；手动Fever要由真实点击产生成功FeverTap并验证出手槽位。不得仅凭窗口关闭、CD变化或截图存在替代命令/事件校验。

【任务二：每场证据】
为每场分配battle_id；开战时冻结初始头，在NEXT/重试替换GameRoot.Battle前保存原场命令、事件、结果。不能最后只导出当前第二场。
保存完整命令Seq/Tick/Kind/Source/Accepted/Reason及事件，关联截图和连续录像。主流程日志可跨场，但战斗日志必须分场。

【任务三：效果实际调用链与导入】
从SkillDef/EffectId实际施放检查ChargeSpeed、ChargeAmount、Barrier的AllAllies目标，不只直接ApplyStatus做单测。
双方技能可用性检查一致：按已声明的设计语义，敌方沉默时不放Tap/Slide、普攻继续。
能力门禁细化到opcode+kind+参数与活动内容依赖闭包，不能因status.apply对其他kind可执行就放行Reflect/Dot等无消费者组合。
允许未知资料保留在非可玩库存，严禁偷偷上场不生效。解析/验证在候选容器进行，成功后一次安装。失败保持此前有效活动数据不变，不先BuildBuiltin或Install再抛错。

【任务四：统一命令与实际回放器】
推荐采用外部输入重放：自动Tap/Slide/Drive/Fever均通过Submit记录，但回放只重投外部输入，自动策略由模拟确定性再生一次。或选择完整命令驱动并关闭生成器；两者不可混用。
开战时固定真实初始auto/speed/profile/seed、角色成长装备leader、时钟与数据身份，不从最终状态猜。覆盖角色数值、技能平值/目标、状态Trigger/PeriodSec、关卡等实质字段，不拿不完整ContentFingerprint当完整版本保证。
让ReplayReport纳入命令拒绝/未消费/版本不匹配，任何Divergences都不能仍Match=true。
用实际BattleReplayer新增自动Fever、非默认初始Speed/Auto和失败命令回归，不以测试中另写的重放循环替代。

【任务五：QTE与数值证据收尾】
修QTE超时恢复tick内TimeLeft双扣，验证speed=1/3以及QTE与关卡不同缩放政策。
主调用者已经使用Computed后，完成Measured旧别名迁移；计算成功不等于实测。更新过渡测试是为了执行已冻结的新语义，不是弱化正确断言。区间结果不能默认为一个整数点值。

【测试与交付】
QA先写可暴露本轮问题的反例，明确哪些已运行、哪些只是源码推导。原有203项保持回归，输出真实TRX，不伪报红转绿。
源码冻结后，在干净的独立工作树构建/重跑，原用户工作区无需清空。提交关联的source_commit/source_tree、数据/配置哈希、实际命令、工具版本、日志、每场事件、录像与可运行构建。未运行写NOT_RUN，环境不足写BLOCKED。
旧报告中d631573脏树为历史证据，不再写成本轮HEAD；新摘要说明当前源码与证据对应关系，不修改历史失败为通过。
没有阻断级回归且规定真实动作矩阵完成，才交PATH_A_ENGINEERING_PASS给用户复核；缺陷未修则NEEDS_FIX。M1_FIDELITY=DEFERRED_NOT_REMOVED，G3_NOT_STARTED。

继续遵守公开搜片/登录拉流/模拟器操作与精确GL数值、T27/T28的延期决定，不把缺参考作为上述工程任务停止的理由。不扩真实养成、World Boss/PVP，不全面更换Live2D，不新增复杂后台。
```

## 就绪任务建议（不是并发上限）

独立QA、自然输入/场景配置、分场证据、效果目标与沉默、导入门禁/原子安装、回放、QTE边界、数值证据迁移、构建验证可分别派发。可继续细分不冲突的任务，完成后回收槽位。共享文件补丁由集成者串行落盘，不限制其他工作并行准备。

本批交付以缺陷关闭和真实运行证据为准，不以新增提交数、字数或常驻代理数为准。
