# G0 / G1 / G2 STATUS

## 本轮（2026-09-13 路 A：换波不再盖 Loki 衔）

工程（≠ 验收）：
- 有图形 Editor 冒烟 **`vs-smoke-timing-20260913d` PASS**
- 普通切片 `IsBoss` 不再播 `THE MASTER OF DESIRE`（那是 P0 t100 Loki，不是 t65 PHASE）
- 目视 `10_wave`：空场 + 心形 + **PHASE 2** + 废都入口；HUD 仍 PHASE 1/2；无 Loki 衔
- 截图 `our_slice/shots_20260913d/`。还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-13 路 A：LEVEL UP + 再战）

工程（≠ 验收）：
- 有图形 Editor 冒烟 **`vs-smoke-timing-20260913c` PASS**。`levelup=True rematch=True`
- 不再 `SkipLive` 掉 CLEAR 后的 LEVEL UP。目视 `12_levelup`：`LEVEL UP!` + `Level 2 ▶ 3` + Confirm（体力/等级数字仍是 stub，不是公式）
- 结算后 `StartVsBattle` 再开一局。目视 `13_rematch`：PHASE 1/2、01:30、五人上场
- 截图 `our_slice/shots_20260913c/`。存档隔离。还原分仍 **0**。T26 工程链更齐，**不是** T26/T27/T28 验收。未开 G3。未声称 M1

## 此前（2026-09-13 路 A：编队链恢复 + PAUSE 重冒烟）

工程（≠ 验收）：
- 有图形 Editor 冒烟 **`vs-smoke-timing-20260913b` PASS**。`screens=` 这次是真走到 **Characters / Team / Inspect**（不再谎称）
- 离开大厅不再 `DestroyImmediate` Inochi（`EmeraldLobby.ReleaseFrom`）。20260911c 编队 120s 卡死按这个修
- 冒烟重新走 Home→契灵→编队→详情→关卡→战斗→CLEAR!!→Home。超时仍可跳过，本轮没跳
- 目视 `06i_pause`：**PAUSE + SPEED ×1/×2/×3 + HOME / CONTINUE / Repeat**，无时钟政策长文
- 目视 `02_roster` / `04_team` / `03_inspect`：五人卡 + 战力 + 冰刃详情（Resonance 壳，不是 GT）
- 截图 `our_slice/shots_20260913b/`。存档隔离。还原分仍 **0**。T26 仍缺养成/再战，**不是** T26/T27/T28。未开 G3。未声称 M1

## 此前（2026-09-13 路 A：PAUSE 卡去掉工程说明）

工程（≠ 验收）：
- 对照 `shots_20260913a/06i_pause`：Robin/P0 暂停卡是 **PAUSE + SPEED 档 + Repeat**，没有时钟政策长文
- 已从 `PauseBoard` 拿掉 `battle clock paused` / `scales with speed…` / `Leaving returns to Home`（仍 inventory）
- 保留 ×1/×2/×3 与 HOME / CONTINUE / Repeat。当时未重跑冒烟。还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-13 路 A：有图形冒烟 PASS）

工程（≠ 验收）：
- 有图形 Editor 冒烟 **`vs-smoke-timing-20260913a` PASS**（`Tundra build success`；首轮 Safe Mode 已杀并重开）
- 链路：Home→Stage→Battle→CLEAR!!→Home。tap/slide/drive/fever/wave/control/speed/auto/pause/kill 都打到。存档隔离
- 目视 `07_drive`：金币 **`PRESS BUTTON`**；无 `HERE IS A POINT!!` / `DAMAGE 150%` / LEADER 大章
- 目视 `06_battle`：无 LEADER 场印。`08_fever`：`FEVER TIME` leftover + 肖像 **WEAKPOINT**
- 截图 `our_slice/shots_20260913a/`。还原分仍 **0**。未开 G3。未声称 M1。T27/T28 仍延期

## 此前（2026-09-12：延期补件签字 + 路 A）

工程（≠ 验收）：
- 用户：「这些我们暂时待定……合同你写清楚后面补」。已签 `m1/G1/DEFERRED_SUPPLEMENT.md` / `.json`
- T27 / T28 / T14–T20 / P0 高清 / P2 / 投放升格 / 模拟器 / 公开搜 = **`DEFERRED_NOT_REMOVED`**（后面补，**不从分母删**）
- `fidelity_pass_without_gt` 仍 **false**。还原分仍 **0**。M1 未验收。未开 G3
- 当前只走路 A：已入库 P0/P1 身份 + 可跑切片。GT 搜 **暂停**（`gt_search/WATCH.md`）
- 指针：`04_ACCEPTANCE_AND_TESTS.md` §1/§6、`TARGET.json` `deferred_supplement`、CUE_SCORECARD 路 A
- 路 A：QTE 不再每场印教程 `HERE IS A POINT!!` / Hard `Skill Addition`（仍 inventory）。币面 `PRESS BUTTON` 保留

## 此前（2026-09-12 GT: 用户 `dc普通战斗关卡.mp4`）

工程（≠ 验收）：
- 新片 `dc实际录制视频素材/dc普通战斗关卡.mp4`：32s，**764×1392**，MuMu 窗录。**WORLD BATTLE 1-1-1 教程**，3 人 + 空槽，KR/CN 文案
- 有普通小怪战 + `PHASE 1/2→2/2` + `勝利/victory` + `CLEAR!!` 三星。顶栏是 **`AUTO SKILL`**（P0 仍是 `FULL AUTO`，不替换）
- **contrast only**。未进 ROOT。窗框 + 非 GL EN ⇒ **不是 T27**。还原分仍 **0**。未开 G3。未声称 M1
- 盘点：`gt_search/23_user_drop_ordinary_wb11.md`

## 此前（2026-09-12 G2: CLICK TIME + SKILL RATE + LEVEL UP ▶）

工程（≠ 验收）：
- Hard SHOWTIME cool：FEVER≈0 → **`LEAD TIME`**（r50）；banked Fever → **`CLICK TIME`**（r67）
- Drive tip 场浮：**`13% SKILL RATE`**（t388；TitleCase → inventory）
- LEVEL UP：**`Level n ▶ m`**（t470）
- CLEAR Stage2 band / FINAL/NEXT/SHOW TIME spaced → inventory；LOCK TIME 仍未证实
- 抽帧 `_frames_g2_cont59/`–`_frames_g2_cont61/`；`dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: LEVEL UP ▶ + CLEAR cont59）

工程（≠ 验收）：
- LEVEL UP 模态：**`Level n ▶ m`**（t470；`>` / `▷` → inventory）
- CLEAR t396 Stage2 band inventory：`2 LEVEL` / `73 GOLD` / `EXP 6/12`（live 仍 t472 stubs）
- CLEAR 星：t396/t472 中星更大；t530 右星更大（软冲突）
- inventory：`NEXT 0.0s TOTAL` / `FINAL n/m` / `IT'S SHOW TIME!!`（Hard OCR；live 仍 PHASE / SHOWTIME）
- r38 SHOWTIME cool 仍 COOL 数字；**LOCK TIME 未证实**
- 前轮：LEAD TIME / SKILL RESERVE / CHILD PHASE / SKILL UP tip / The Ranger
- 抽帧 `_frames_g2_cont59/` `_frames_g2_cont60/`；`dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: LEAD TIME + CHILD PHASE + SKILL RESERVE）

工程（≠ 验收）：
- Hard SHOWTIME cool：**`N LEAD TIME`**（r50；非 Fever）
- Hard 近 Fever SHOWTIME tray：**`WEAKPOINT` + `SKILL RESERVE`**（r15）
- PHASE splash 低级：**`CHILD HP TOTAL`**（t280/t340；SkillReady tip 前）；之后 / cap：**`SKILL HP TOTAL`**（t372/t065）；tips 后：**`TEAM`+(CURRENT)**（t510）
- Childs tip 底弧：**`SKILL HP TOTAL`**（t250）；SkillReady tip：**`SKILL UP TOTAL`**（t352）
- Fresh Skull 顶条：**`The Ranger`**（t88）；`ENEMY TEAM HP TOTAL` / `NEARLY TOTAL` inventory
- 死亡窗 cont57 仍无 KO 印；抽帧 `_frames_g2_cont55/`–`_frames_g2_cont58/`；`dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 GT: 自搜素材 — KR 故事战 contrast）

工程（≠ 验收）：
- B 站 `BV18c411W7dT`（2023-04-03，**886×1920**，74.5s）：韩文 UI 普通故事/教程 PVE（PHASE / FULL AUTO / FEVER 0%）。**contrast only**，未进 ROOT
- 分类：`gt_search/22_bili_kr_story_pve_BV18.md`；片：`contrast/kr_story_pve_202304_BV18c411W7dT.mp4`
- 模拟器已点到 `WORLD BATTLE` 1-2 出战页；1st PARTY 空槽，未录成战斗。YouTube/Archive TLS 仍不通。无新 GL EN 主 GT
- 还原分仍 **0**。未开 G3。未声称 M1。P2 / ≥720p GL 仍缺

## 此前（2026-09-12 G2: TOTAL HP TOTAL + ENEMY HP LEFT + Skill Rate）

工程（≠ 验收）：
- Total HP tip 底弧：**`TOTAL HP TOTAL`**（t345；SHARED 退 inventory）
- Ordinary SHOWTIME 顶条：**`ENEMY HP LEFT`**（t360）
- Drive tip 底短条：**`SKILL GAUGE`**（t365）；tip 场浮 **`13% Skill Rate`**（t388）
- 前轮：PHASE `(CURRENT)` / White Veil / SKILL UP TOTAL
- 抽帧 `_frames_g2_cont54/`；`dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: PHASE splash TOTAL/(CURRENT)）

工程（≠ 验收）：
- PHASE splash 顶条：**`ENEMY HP TOTAL` + `ENEMY HP (CURRENT)`**（t510）
- PHASE splash 底条：**`TEAM HP TOTAL` + `TEAM HP (CURRENT)`**（t510）
- 前轮：Pomona **`White Veil`**；cap **`SKILL UP TOTAL`**；LOCK TIME 未证实
- 抽帧 `_frames_g2_cont53/`；`dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: Pomona White Veil + SKILL UP TOTAL）

工程（≠ 验收）：
- Pomona Slide stub：**`White Veil`**（Hard r62 / cont52 t62.5）；`Saint's Blessings` 退回 inventory
- Cap 故事底副条：**`SKILL UP TOTAL`** 0% 字（t64；弧仍跟 Drive fill）
- LOCK TIME：cont38/52 密扫仅见 **COOL TIME** — 保持 inventory/OCR 未证实
- t64 击杀窗仍无 KO；PHASE 3 splash（t96）已见
- 抽帧 `_frames_g2_cont52/`；`dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: FEVER TIME cool + DMG OF TOTAL）

工程（≠ 验收）：
- Hard Fever+SHOWTIME 肖像 cool：**`N FEVER TIME`**（r67）；就绪仍 **`WEAKPOINT`**
- Hard Fever 顶副条：**`DMG OF TOTAL`**（r67）；SHOWTIME 非 Fever 仍 **`ENEMY DRIVE`**
- Sytry 板标题恢复 **`Lovey-Dovey`**（r42/r47）
- 前轮：LEVEL UP `n > m` / Fever tip art / Skill Addition
- 抽帧 `_frames_g2_cont51/`；`dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: LEVEL UP arrow + Fever tip art + Skill Addition）

工程（≠ 验收）：
- LEVEL UP 箭头：**`n > m`**（t470）；Fever rates tip art：**`FEVER TIME!!`** + 场浮 **`TOTAL 427 DAMAGE`**
- Fever tip 窗底条：**`SKILL HP TOTAL`**（t435）；Hard PERFECT 浮字：**`Skill Addition`**（r56）
- Inventory：Mona VN wipe 台词（t120）；`>> SKIP` 单行
- 抽帧 `_frames_g2_cont50/`；`dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: Tip dual MY/PARTY + Hard QTE ENEMY HP）

工程（≠ 验收）：
- Cap 故事 tip 窗底条：**`MY HP TOTAL` / `PARTY HP TOTAL`** 叠字（P0 t80）
- Hard QTE 顶条：**`ENEMY HP`**（r55）；`QteDamageWord` **`DAMAGE`**
- Inventory：`The Ranger` / `SKILL UP TOTAL`；Skull 仍 **`The Hanged`**
- 死亡窗仍无 KO；抽帧 `_frames_g2_cont49/`；`dotnet test` **156**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: Hard QTE ENEMY HP short + PERFECT DAMAGE）

工程（≠ 验收）：
- Hard QTE 窗顶条：**`ENEMY HP`**（r55；非 TOTAL）；`VfxJudge` **`DAMAGE`** + `N%`
- Inventory：`The Ranger` / `SKILL UP TOTAL`（t88 OCR）；Skull 顶标仍 **`The Hanged`**（t83）
- 前轮：Auto chip / Sytry Lovey-Dovey / TipSpeed
- 死亡窗 cont49 仍无 KO 印；抽帧 `_frames_g2_cont49/`
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: Auto badge + The Ranger + Sytry Lovey-Dovey）

工程（≠ 验收）：
- Auto 施放：有 stub → **`Auto`** chip + 技能名（t500）；无 stub → **`AUTO SKILL`**（r36）
- Inventory：`The Ranger` / **`SKILL UP TOTAL`**（t88）；Sytry **`Lovey-Dovey`**（r48）
- 前轮：TipSpeed / TipSlidePower 队列 / Pixie Dark Water
- 抽帧 `_frames_g2_cont48/`；`dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: TipSpeed + TipSlidePower queue + Pixie Dark Water）

工程（≠ 验收）：
- Mona Tips 速度条：**`TipSpeedBody`**（P0 t451 全文；X1 SPEED 高亮）
- Slide tip 队列：t355 Mona → t370 **`TIP!` TipSlidePower**（拆 `_tipSlideMonaShown`）
- Pixie Auto stub：**`Dark Water`**（t452；与衣装同字）；Tamogami Slide **`Desperation`**
- Inventory：Hades VN 台词 t74；死亡窗仍无 KO 印
- 抽帧 `_frames_g2_cont47/`；`dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: Death Patch + Hades VN + Mona Eclipse）

工程（≠ 验收）：
- Inventory：Hades **`Death Patch`**（t74）；Davi 台词 **`Okey-do.`**（t78）；`NOW LOADING...` / `SilentHunter`
- Mona Auto：**`Eclipse`**（t500/t456）
- 死亡窗 t74–t85 仍无 KO 印；抽帧 `_frames_g2_cont45/`
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: Mona Auto Eclipse + SilentHunter inventory）

工程（≠ 验收）：
- Mona Auto stub：**`Eclipse`**（P0 t500/t456；与衣装同字）
- Inventory：`SilentHunter`（r51 compact）、`NOW LOADING...`（r72）
- 抽帧 `_frames_g2_cont43/` `_frames_g2_cont44/`
- 前轮：Drive-tip cool `SKILL TIME`；`Royal Hellboard`
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: Drive-tip cool SKILL TIME + Royal Hellboard）

工程（≠ 验收）：
- Drive tip / SHOWTIME 肖像 cool：**`SKILL TIME`**（P0 t365 + Hard r67）；中段仍 **`COOL TIME`**
- 前轮：PERFECT `Live Skill Addition`；`AUTO SKILL`；Sakuragawa **`Royal Hellboard`**
- 抽帧 `_frames_g2_cont42/`；`dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: Live Skill Addition + Royal Hellboard + AUTO SKILL）

工程（≠ 验收）：
- PERFECT 场浮字：**`Live Skill Addition`**（r56）；Auto 肖像：**`AUTO SKILL`**（r36）
- Sakuragawa Slide stub：**`Royal Hellboard`**（r49）；Inventory：`DARKNESS TOTAL`（r36）
- Bleed EN 注释对齐；抽帧 `_frames_g2_cont41/`
- 前轮：SHOWTIME cool `SKILL TIME`
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: SHOWTIME cool → SKILL TIME）

工程（≠ 验收）：
- SHOWTIME 肖像 cool：**`SKILL TIME`**（Hard r67）；`SLIDE TIME` 退回 inventory
- Inventory：`Tap the screen...`（t390）；抽帧 `_frames_g2_cont40/`
- 前轮：Pixie `Freeze Lance`；Drive Ready → `DRIVE TOTAL`
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: Freeze Lance Pixie Slide）

工程（≠ 验收）：
- SHOWTIME Slide stub：Pixie → **`Freeze Lance`**（P0 t360 RANK 1）
- 前轮：Drive Ready → `DRIVE TOTAL`；Wedding Bells；LOCK TIME inventory
- 抽帧 `_frames_g2_cont39/`；`dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: Drive Ready → DRIVE TOTAL）

工程（≠ 验收）：
- Drive ≥100 且非 Fever/SHOWTIME/PHASE：底条 **`DRIVE TOTAL`**（Hard r48 + tip t388）
- Inventory：**`REVEAL`**（r12 Oracle Werewolf overhead）
- 前轮：Wedding Bells / LOCK TIME / Snake Bite confirm
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: Wedding Bells + LOCK TIME + Snake Bite confirm）

工程（≠ 验收）：
- Pomona Auto stub：**`Wedding Bells`**（r22 NamePop）；Tiamat SHOWTIME **`Snake Bite`** 再确认（r38）
- Inventory：**`LOCK TIME`**（r38 双 cool；第二钟 UNKNOWN）
- 抽帧 `_frames_g2_cont38/`；前轮 Saint's Blessings / Hard ENEMY DRIVE / CLEAR Back
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: White Veil + Hard SHOWTIME ENEMY DRIVE）

工程（≠ 验收）：
- SHOWTIME Slide stub：Pomona → **`Saint's Blessings`**（robin_t45）；**`White Veil`** 退回 inventory（r62 变体）
- Hard SHOWTIME 顶副条：**`ENEMY DRIVE`**（r62）；Hard 中段仍 **`PREPARATION`**（r70）
- Hard SHOWTIME 底条 PARTY 判定含 `VfxShowtime.AnyLive()`
- 前轮：CLEAR Retry/Back / Silent Hunter / Robin Justice / Black Widow
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: CLEAR Back/Retry + Silent Hunter + Hard PARTY）

工程（≠ 验收）：
- CLEAR 右轨：**Retry / Back**（t472）；黄带账号 **`GameplayHe`**；Valkyrie 衣装 **`Black Widow`**（t501）
- DriveCrush stub：Sakuragawa → **`Silent Hunter`**（r52）；SHOWTIME Slide：Robin → **`In the Name of Justice`**（r50）
- Hard SHOWTIME / QTE judge 底条：**`PARTY HP TOTAL`**（r50/r55）；Hard 中段仍 **`MY HP TOTAL`**（r70）
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: SHARED / DRIVE TOTAL / PREPARATION / CURRENT HP）

工程（≠ 验收）：
- Team HP tip：**`SHARED HP TOTAL`**（t345）；Childs tip：**`CURRENT HP`**（t250）
- Drive Tables tip 窗：**`DRIVE TOTAL`**（t388）；Hard 顶副条 **`PREPARATION`** + 场上 **`EVA`**（r61）
- CLEAR 三星右升弧（t395）；TipChilds 补全 “For now, it's just me.”
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: SHARED HP / DRIVE TOTAL / PREPARATION / EVA）

工程（≠ 验收）：
- Team HP tip 窗底条：**`SHARED HP TOTAL`**（t345）
- Drive Tables tip 窗底条：**`DRIVE TOTAL`**（t388）
- Hard 顶副条：**`PREPARATION`**（r61）；场上 **`EVA`** float
- CLEAR 三星：右升弧右星更大（t395）
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: PARTY HP TOTAL + Filled with Love）

工程（≠ 验收）：
- Drive tip 后未满级底条：**`PARTY HP TOTAL`**（t380）
- Drive 技能名 stub：Mona → **`Filled with Love`**（t382）；Slide：Tiamat → **`Snake Bite`**（r67）
- 前轮：PHASE CHILD / post-Fever MY / Mona Tips slide / LEVEL UP ▶
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: PHASE CHILD + Mona Slide + LEVEL UP ▶）

工程（≠ 验收）：
- 未满级 PHASE splash：**`CHILD HP TOTAL`**（t446）；Fever 会话后未满级中段：**`MY HP TOTAL`**（t456）
- TipSlidePower 链：**Mona's Tips** + `Slide Skills are powerful attacks!`（t355）
- LEVEL UP：`Level 2 ▶ 3` stub（t470）
- 前轮烟测 `20260912n` Fever 肖像 WEAKPOINT PASS（切片）
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12：烟测 `20260912n` 核过 Fever 肖像）

工程（≠ 验收）：
- `vs-smoke-timing-20260912n` **PASS**。`08_fever` 肖像是 **`WEAKPOINT`**，不再是全员 `DRIVE SKILL READY`；彩虹条 + leftover + 底栏 `FEVER 0%`
- 仍是我方切片。还原分 **0**。未开 G3。未声称 M1。P2 / ≥720p 仍缺

## 此前（2026-09-12 G2: Fever 肖像 WEAKPOINT 优先）

工程（≠ 验收）：
- Fever 窗肖像：`WEAKPOINT` 压过 `DRIVE SKILL READY`（P0 t442/t452；`08_fever` 曾全员 Drive Ready）
- QTE：场上不再叠第二行 `PRESS BUTTON`（t382 字在钮上）
- 无新 ≥720p / P2 / 普通 5p PVE。还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: CHILD HP by party level）

工程（≠ 验收）：
- 底条 **`CHILD HP TOTAL`**：wave0 **或** 存档队伍未满级（t512 Lv.1 PHASE 2）；满级中段 / PHASE splash 仍 `SKILL HP TOTAL`；Hard 仍 `MY`
- 前轮：烟测 `20260912m` PASS；CLEAR `Total 2`；DriveCrush `DRIVE SKILL`；TipKeepAttacking
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12：切片烟测 PASS `20260912m`，仍 ≠ M1）

工程（≠ 验收）：
- PlayMode `vs-smoke-timing-20260912m` **PASS**：CLEAR!! → HOME；tap/slide/drive/fever/wave/control/speed/auto 都打到
- Result 不再卡 180s。`06c` SHOWTIME 印 `RANK — LV —/—`（空技能槽，未编 RANK 1）
- 存档隔离 `client/Temp/vs-smoke-save/`。截图 `our_slice/shots_20260912m/`
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺。T27 仍 BLOCKED

## 此前（2026-09-12 G2: CLEAR Total stub）

工程（≠ 验收）：
- CLEAR 顶：**`Total 2`** stub（t398；计数 UNKNOWN）
- 前轮：PHASE party `SKILL HP TOTAL`；DriveCrush `DRIVE SKILL`+名；TipKeepAttacking；dual `+/SLIDE`；Boss Desire
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: PHASE SKILL HP + DriveCrush skill badge）

工程（≠ 验收）：
- PHASE splash 底条 party：**`SKILL HP TOTAL`**（death60 t65 / t096）；wave0 仍 `CHILD`；Hard 仍 `MY`
- DriveCrush：`DRIVE CRUSH` + **`DRIVE SKILL`** + 技能名（r52）；TipKeepAttacking（t60）；dual `+/SLIDE`；Boss Desire intro
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: DriveCrush DRIVE SKILL + skill name）

工程（≠ 验收）：
- Drive 切镜：`DRIVE CRUSH` + **`DRIVE SKILL`** badge + 技能名（Hard r52 `Silent Hunter`）；无技能名时仍印 CN `碾压`
- 前轮：TipKeepAttacking（t60）；early PHASE `CHILD HP TOTAL`；PHASE `DRIVE GAUGE`；dual `+/SLIDE`；Boss Desire/WARNING/Dark Prince
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12：烟测未重跑 — Editor Safe Mode 冻住）

- 监视 `editor-playmode-timing.log` 180s：**0 新字节**（mtime 仍 15:52）。进程 50112 仍占工程锁，停在 CS0136 进的 Safe Mode
- CS0136（`BattleHud` 里第二个 `var hard`）**磁盘已修**。RANK 槽 / 去 TAP READY / Result 松手也在盘上
- **未**写 `vs-smoke.request`（编译未 FinalizeReload）。还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: TipKeepAttacking + early PHASE CHILD HP）

工程（≠ 验收）：
- Tip 链：Tap → **`Keep using your skills and attack the enemy!`**（t60）→ Team → Childs…
- 早期 PHASE splash（t280）：底条仍 **`CHILD HP TOTAL`**；后期 PHASE（t096）仍 `SKILL HP TOTAL` / `DRIVE GAUGE`
- 前轮：dual `+/SLIDE`、Boss Desire/WARNING/Dark Prince、PHASE DRIVE GAUGE
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: PHASE DRIVE GAUGE + dual SLIDE pip + Boss intro）

工程（≠ 验收）：
- PHASE splash 底条：**`DRIVE GAUGE`**（p0_t096）；普通 PHASE 仍 **`SKILL HP TOTAL`**（Hard 才 `MY HP TOTAL`）
- 肖像 ready：slide 就绪叠 **绿 `+` / 红 `SLIDE`**（Hard r58）
- Boss intro：`THE MASTER OF DESIRE` / `BOSS` / `WARNING` / Loki→`Dark Prince`（t100）
- `dotnet test` **156**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: SHOWTIME RANK 按技能槽，不按等级）

工程（≠ 验收）：
- SHOWTIME RANK：**不**从单位等级推。r50 `RANK 1 LV 10/10` 与 r62 / PVP5 t090 `RANK 7 LV 10/10` 同是满 LV。空内容印 `RANK — LV —/—`。`SkillDef.SlideRank/Lv/LvMax` 给外部稿填
- 头像去掉自造 `TAP READY` / `SLIDE READY` 字（P0 t446 / PVP5 是绿 `+` / `SLIDE`）
- Result 烟测：不再等 LevelUp 4s；截图卡住会松；禁止第二套 smoke runtime
- PVP5 / Raid / WB **不升格**。还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: Tiamat costume + SKILL GAUGE TOTAL）

工程（≠ 验收）：
- 肖像 costume：Tiamat → **Primordial Serpent**（r65）
- 底条 **`SKILL GAUGE TOTAL`**（t500）；QTE **`HERE IS A POINT!!`**；Drive tip Tables 链上
- `dotnet test` **155**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: SKILL GAUGE TOTAL + HERE IS A POINT）

工程（≠ 验收）：
- 底 Drive 条：**`SKILL GAUGE TOTAL`**（P0 t500）；SHOWTIME 仍 `DRIVE GAUGE`
- QTE 开窗：**`HERE IS A POINT!!`**（t382）
- Tip：SkillReady 标题拆分；DriveSkill → Timing → **Tables**
- `dotnet test` **155**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: TipTap + TipDriveSkill order）

工程（≠ 验收）：
- Tip 序：**Tap** → Team → Childs → Skill → Slide → DriveSkill → DriveTiming → Fever
- Drive 首 tip 用 **`TipDriveSkill`**（t380）；`TipDriveTables` inventory
- Werewolf / SHOWTIME RANK / CHILD / WEAKPOINT
- `dotnet test` **155**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: TipTap first + Werewolf plate）

工程（≠ 验收）：
- Tip 队列改：**TipTap**（t58）→ Team HP → Childs → Skill → Slide → Drive → Fever
- 敌牌 Dracula Werewolf 不强制 Oracle；SHOWTIME RANK；CHILD/WEAKPOINT
- KR 私服 / Raid/WB/PVP5 **≠** primary
- `dotnet test` **155**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: Dracula Werewolf + tip/RANK）

工程（≠ 验收）：
- 敌牌：`Dracula Werewolf` 不强制 Oracle（r50）；Neamhain/Green Victorix 保留
- Drive tip Timing 链；SHOWTIME RANK；CHILD HP；WEAKPOINT；TipSlidePower
- KR 私服 / Raid/WB/PVP5 **≠** primary
- `dotnet test` **155**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12：不在 MuMu 上起 1.1 私服）

- 用户 MuMu 是韩服官端通信失败。整合包仍是分卷 zip（jar + 改 `server_addr`）
- **未**解压、**未**起 MySQL/jar、**未**改模拟器。KR 私服 ≠ GL 普通 PVE 主 GT
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: TipDriveTiming + SHOWTIME RANK）

工程（≠ 验收）：
- Drive tip 链：`TipDriveTables` → **`TipDriveTiming`**
- SHOWTIME **`RANK 1 LV 10/10|1/10`**；wave0 **`CHILD HP TOTAL`**；**`WEAKPOINT`**
- `dotnet test` **155**；还原分仍 **0**。未开 G3。未声称 M1。P2 仍缺

## 此前（2026-09-12 G2: SHOWTIME RANK + CHILD/WEAKPOINT）

工程（≠ 验收）：
- SHOWTIME rank：cap **`RANK 1 LV 10/10`**（r50）；教程 lv1 **`1/10`**；中档仍 stub
- Wave0 **`CHILD HP TOTAL`**（t448）；Fever 肖像 **`WEAKPOINT`**；`TipSlidePower`
- Raid/WB/PVP5 用户丢片 **不升格**
- `dotnet test` **155**；还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: CHILD HP + WEAKPOINT + TipSlidePower）

工程（≠ 验收）：
- Wave0 底条：**`CHILD HP TOTAL`**（P0 t448）；wave1+/Fever 后仍 `SKILL HP TOTAL`
- Fever 肖像戳：**`WEAKPOINT`**（t452；spaced 退 inventory）
- 二次 Slide tip：`TipSlidePower`
- 用户丢 Raid/WB/PVP5 **不升格** primary
- `dotnet test` **155**；还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12：用户丢 Raid / WB20 / PVP5）

- 路径 `dc实际录制视频素材\`：`dc.raid.mp4` / `dc.wb.mp4` / `dc.5人pvp.mp4`。未改名，未进 primary 根
- **PVP5**（434×920，GL EN 竞技场）：站位/底栏/SPEED/Fever/COOL TIME/`SLIDE SKILL RANK 7 LV 10/10` 可当普通 5 人对位几何对照。`PHASE 1/1`+`ESCAPE`+竞技场结算 **不是** 普通 PVE
- **Raid** 单 Boss + 繁中；**WB** 约 20 人头 + `FULL AUTO` + 缺文案。模式差，不升格
- 三支宽度都 &lt;720。还原分仍 **0**。未开 G3。未声称 M1。未关 T27
- 抽帧 `_frames_user_drop/`；说明 `gt_search/21_user_drop_raid_wb_pvp.md`

## 此前（2026-09-12 G2: Drive tables tip + Fever gauge）

工程（≠ 验收）：
- Drive 首次 tip 改 **`TipDriveTables`**（更长 body）
- Fever ≥40% tip；Hard Stage 8 arch；Green Victorix；`|| PAUSE`
- `dotnet test` **155**；还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: Fever gauge tip + Hard Stage 8）

工程（≠ 验收）：
- Fever 条 ≥40% 出一次 tip（`TipFeverGaugeBody` / t420）
- Hard 拱顶 **`Stage 8 You Won't Get Away! (Hard)`**；Green Victorix；`|| PAUSE`
- Tip 队列 + CLEAR stubs；Skill Boost；`dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: Hard Stage 8 arch title）

工程（≠ 验收）：
- Hard 拱顶 frame-lock：**`Stage 8 You Won't Get Away! (Hard)`**（r30/r70）；修多词截断
- Green Victorix；Hard `|| PAUSE` + Repeat；Skill Boost；tip/CLEAR
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: Green Victorix + || PAUSE）

工程（≠ 验收）：
- 敌牌：**Green** / `N Victorix`（Hard r30）
- Hard **`|| PAUSE`** + 场 **Repeat**；Skill Boost；tip 队列；CLEAR stubs
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: Hard || PAUSE）

工程（≠ 验收）：
- Hard 场暂停字改回 **`|| PAUSE`**（r30/r70）；`II PAUSE` inventory
- Hard 场 **Repeat**；Skill Boost chips/float；tip 队列；CLEAR stubs
- Inventory：`HARD_RAIL_TABS`（r30 右轨 5/3/8）
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: Skill Boost float + Hard Repeat）

工程（≠ 验收）：
- Buff float 映射 **Slide/Tap/Drive Skill Boost**（r70）；chip 同文案
- Hard 场 **Repeat**；`BATTLE NO.` inventory-only
- Tip 队列 + CLEAR stubs/rail；Fever tip TOTAL art
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: Hard Repeat + Slide Skill Boost）

工程（≠ 验收）：
- Hard 场上常驻 **Repeat**（r70）；`BATTLE NO.` 退回 inventory（r70 仍 `BATTLE TIME`）
- Chip：**`Slide Skill Boost`**（及 Tap/Drive Skill Boost）对 r70
- CLEAR 右轨 BOSS/RETRY/HOME；tip 队列；CLEAR 黄带 stubs
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: CLEAR rail + Hard clock）

工程（≠ 验收）：
- CLEAR 右轨 stub：**BOSS / RETRY / HOME**（t472）；黄带 3/6/70 + LEVEL UP
- Hard：**`BATTLE NO. 1`** + QTE 场 **Repeat**（r55）
- Tip 队列 Team→Childs→Skill→Slide/Drive→Fever(+TOTAL art)
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: Hard BATTLE NO + QTE Repeat）

工程（≠ 验收）：
- Hard 时钟副字：**`BATTLE NO. 1`**（Robin r55）；普通仍 `BATTLE TIME`
- Hard QTE 场上 **Repeat**（r55 under PERFECT）；普通场仍藏
- Tip 队列 Team→Childs→Skill→Slide/Drive→Fever；CLEAR stubs
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: TipSkillReady + tip queue）

工程（≠ 验收）：
- Tip 队列：Team HP → Childs → **TipSkillReady**（首次 tap）→ Slide/Drive → Fever
- CLEAR t472 stubs；肖像 **`+`** / **`WEAK POINT`**；PHASE **`MY HP TOTAL`**
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: tip queue Team→Childs→Slide/Drive/Fever）

工程（≠ 验收）：
- Tip 链：`Total HP` → `Mona's Tips`/`Childs tray`；Slide/Drive 首次就绪；Fever activate→rates+TOTAL art
- CLEAR t472 stubs；肖像 **`+`** / Fever **`WEAK POINT`**；PHASE **`MY HP TOTAL`**
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: Tip Team HP + Slide/Drive）

工程（≠ 验收）：
- 开战 ~1.35s 后 tip **`Total HP`**（t345 body）
- 首次 Slide/Drive tip；Fever rates + TOTAL art；CLEAR stubs；肖像 **`+`** / **`WEAK POINT`**
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: TipSlide/Drive plates）

工程（≠ 验收）：
- 首次 Slide/Drive 就绪各出一次 tip 板（`TipSlideSkill` / `TipDriveTiming`）
- Fever tip rates + **`TOTAL 427 DAMAGE`** art；CLEAR t472 stubs；肖像 **`+`**
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。未关 T27/T28

## 此前（2026-09-12 G2: tip TOTAL art + CLEAR stubs）

工程（≠ 验收）：
- Fever rates tip 加 tip-field **`TOTAL 427 DAMAGE`** 艺术字（t444）+ ChildNote
- CLEAR 黄带 t472 stubs；肖像就绪 **`+`**；PHASE **`MY HP TOTAL`**
- 场上藏 DPS/SKIP/Repeat
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。未关 T27/T28

## 此前（2026-09-12 G2: CLEAR t472 + portrait +）

工程（≠ 验收）：
- CLEAR 黄带：frame-locked **`3/6/70`** + **`EXP 0/18`** + **`LEVEL UP`** chip（t472；≠ 公式）
- 肖像就绪绿 **`+`**（t446）；slide 就绪仍 `SLIDE`
- Fever tip rates chain；PHASE **`MY HP TOTAL`**；portrait **`WEAK POINT`**
- 同窗：场上藏 DPS/SKIP/Repeat（对 t440）
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。未关 T27/T28

## 此前（2026-09-12 G2: 场上藏 DPS/SKIP/Repeat）

工程（≠ 验收）：
- P0 t440 实战无左侧 `TOTAL/DPS/HEAL` 盒、无场中 Repeat、无 SKIP/Log。`VfxDpsPanel` 改为 Hide；Repeat 只留 PauseBoard
- 无新 P0 高清 / P2 实文件。还原分仍 **0**。未开 G3。未声称 M1。未关 T27/T28

## 此前（2026-09-12 G2: tip rates chain + PHASE MY HP）

工程（≠ 验收）：
- Fever tip：`Activate` → chain **rates** 板（t445 `TipFeverBarrage` / PERFECT 40%…）
- PHASE splash 底条：**`MY HP TOTAL`**（P0 t446；`TEAM HP TOTAL` 退回 inventory）
- 此前：`VfxTipPlate` / portrait **`WEAK POINT`** / DPS live **`n DAMAGE`**
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: Fever tip plate + WEAK POINT + DPS no TOTAL）

工程（≠ 验收）：
- Fever 切入：`TIP!` + `Activate Fever Time!` / 14s body（P0 t442）`VfxTipPlate`
- Fever 肖像戳：**`WEAK POINT`**；左上 DPS 板 live 改 **`n DAMAGE`**（去掉 TOTAL；tip 仍用 `TipTotalDamageLine`）
- Inventory：`OWN HP TOTAL`
- 此前烟测 k：StatusChipText 只认 Id；`08_fever` leftover OK；`06b` 无红斩
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: StatusChipText 编译修复 + 烟测 k）

工程（≠ 验收）：
- 第一次烟测 **COMPILE FAIL**：`StatusChipText` 读了不存在的 `EffectDef.Name`。已改成只认 `Id`
- 第二次有图形烟测 **PASS**（`vs-smoke-timing-20260912k.txt`）。`08_fever`：`FEVER TIME` + `13.98`，底栏 `FEVER 0%`。`06b` 无红斩线。左上 DPS 板仍 `TOTAL n DAMAGE`
- 截图副本：`our_slice/shots_20260912k/`
- 还原分仍 **0**。未开 G3。未声称 M1。未关 T27/T28

## 此前（2026-09-12 G2: SLIDE TIME + CRT Rate + Fever combo）

工程（≠ 验收）：
- SHOWTIME 冷却：**`N SLIDE TIME`**（Robin r50）；平时仍 `COOL TIME`
- Chip：**`CRT Rate ↑`**（P0 t505）；Fever 连击条 live 用 **`n DAMAGE`**（无 TOTAL；t442）
- `WEAK POINT` portrait inventory；抽帧 `_frames_p0_cont37`
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: Fever 对 t440，不从登录会话下片）

工程（≠ 验收）：
- P0 t440/t442 实战：彩虹条 + **`FEVER TIME`** + leftover **`12.50`/`10.83`**（无 `s`）；底栏仍 **`FEVER 0%`**。去掉 HUD 倒计时、`狂热时间` 章、边缘斩线、连击金盒、`TOTAL n DAMAGE`（那是 t444 提示板）
- Tap：去掉连线，只留短拳+环（P0 仍无孤立 tap VFX）
- 用户 Chrome CDP 已登录，P2 `IRDqNAhKKr4` **playability=OK、原生 1280×720、2756s**。未从该会话自动拉流。KIT 根目录仍无真实 P2 文件
- 还原分仍 **0**。未开 G3。未声称 M1。未关 T27/T28

## 此前（2026-09-12 G2: live Fever countdown + Flanger inventory）

工程（≠ 验收）：
- Live Fever（P0 t440）：场上 **`FEVER TIME`** + **`N.Ns`** 倒计时；HUD 发烧条显示 leftover；tip 仍 `FEVER TIME!!`
- CLEAR 奖励名：**Silent Pixie** / **Mona**（t396）；costume inventory `Primordial Serpent`
- Inventory：`REMAINING TIME`（t88）、`The Flanger`、`FINAL TIME N`（r58）；抽帧 `_frames_p0_cont36`
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。未关 T28

## 此前（2026-09-12 G2: PRESS BUTTON QTE + Drive timing tip）

工程（≠ 验收）：
- QTE 钮：**`PRESS BUTTON`**（P0 t382）；Cue 子字对齐
- Tip inventory：`TipDriveTiming` / body；Pause 去掉重复 PAUSE 行
- 此前同窗：Tap 去 Slash；LEVEL `n > m`；`...`/`Log`；PHASE **`TEAM HP TOTAL`**
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 collect-7: 再搜一次，没有新 primary）

- 昨天已经收过：B 站 1–6 轮 + Chrome 录 P0/P1。P2 仍是 768KB 空壳
- 本轮按三条 YouTube 直链去搜 B 站 / archive.org：**无镜像**。API 32 条天命相关里，新 BV 全是 Raid/WB/塔防/教程/乐队 MV
- 未再对 YouTube 跑 yt-dlp。未把 contrast 升格。还原分仍 **0**

## 此前（2026-09-12 G2: Tap no-slash + LEVEL > + Log stack）

工程（≠ 验收）：
- Tap：去掉 `CombatFeel.Slash` / `VfxProjectile` Slash 条；只留软拳 `VfxTapSkill`（对 06b 红斩线）
- LEVEL UP 箭头：`n > m`（P0 t470）
- VN：**`...` / `Log`** 叠字（t400）
- PHASE splash 底条：**`TEAM HP TOTAL`**（t510）；TipSkillReady inventory；抽帧 `_frames_p0_cont35`
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。未关 T27/T28

## 此前（2026-09-12 G2: no leftover 开演 on Drive-ready）

工程（≠ 验收）：
- 有图形 win 冒烟 **PASS**（`vs-smoke-timing-20260912j.txt`）
- `06d`：无 `DRIVE SELECT` 板、无 HUD `开演`；肖像 `DRIVE SKILL READY`
- `06c`：`IT'S SHOWTIME!!` + `SLIDE SKILL` + `RANK 1 LV —/—`
- `06b`：仍有一条红线（Tap 短拳未完全摆脱斩感）。P0 孤立 tap VFX 仍缺
- 副本：`our_slice/shots_20260912j/`
- 合同还原分仍 **0**。未开 G3。未声称 M1。未关 T27/T28

## 此前（2026-09-12 G2: Fresh/Seething foe titles）

工程（≠ 验收）：
- 敌牌：`Fresh`/`Old`/`Seething` 叠字（P0 t70/t83）；去掉 skull `Lv.` 特例
- 此前 continue-41：COOL TIME / Hard MY HP / tip inventory / 藏 ESCAPE
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。

## 此前（2026-09-12 G2: Drive = tap icon, no plate）

工程（≠ 验收）：
- 有图形 win 冒烟 **PASS**（`vs-smoke-timing-20260912i.txt`）。`06d` **1080×1920**
- 目视：无 `DRIVE SELECT` 弹板；底栏肖像 `DRIVE SKILL READY`；Drive 条 100%。对得上 P0 t365/t380「tap the icon」
- `auto-hit seen=True`；`06f_kill` 仍无击破章
- 副本：`our_slice/shots_20260912i/`。仓库根 EternalVow 768KB 仍不是 P2
- 合同还原分仍 **0**。未开 G3。未声称 M1。未关 T27/T28

## 此前（2026-09-12 G2: COOL TIME + tip inventory + Hard pause）

工程（≠ 验收）：
- 冷却肖像回到 **`N COOL TIME`**（P0 tip t370/t380、Robin r45/r50；`SKILL TIME` 仍 inventory）
- Hard：`II PAUSE`；底条 **`MY HP TOTAL`**（r25）；顶次条仍 **`ENEMY DRIVE`**（r62）；藏 **ESCAPE**；关卡名加 **`(Hard)`**
- ND 敌牌叠字 **`Title` / `N Name`**（r25/r35/r68）
- Party 条叠字 `PctOverLabel`；Inspect 属性 EN（HP/ATK…）
- Tip inventory：Slide/Drive/Team HP + Fever rates + Childs tray（t250）；抽帧 `_frames_p0_cont33`/`cont34` / `_frames_p1_cont17`
- PHASE splash 关卡短名 + Hard `(Hard)`
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: auto-hit + enemy death shots）

工程（≠ 验收）：
- 有图形 win 冒烟 **PASS**（`vs-smoke-timing-20260912h.txt`）。截图 **1080×1920**
- `06j_auto_hit`：右敌伤害 `31,833` + 光束；日志 `auto-hit casts=3`。**未**在画面印 Auto 通道字。`06h_auto` 顶栏是 `FULL AUTO`
- `06f_kill`：右敌残影 + 叠伤害数字；`WavePreview.kind=None`；**无** `击破` / KO / DOWN。对得上 P0 中段击杀「只有数字」
- 副本：`our_slice/shots_20260912h/`。存档仍隔离 `Temp/vs-smoke-save/`
- 合同还原分仍 **0**。未开 G3。未声称 M1。未关 T27/T28。GT 输入→命中列仍 UNKNOWN

## 此前（2026-09-12 G2: Log + boss arch + SKILL TIME）

工程（≠ 验收）：
- VN：**`Log`** 在 SKIP 下（P0 t400）
- Hard 顶栏 boss 名大写（活 boss）；敌牌 `Hero` 用于 Robin 镜像
- 冷却肖像：**`N SKILL TIME`**（r50/t365；`COOL TIME` 仍 inventory）
- Inventory：`TipTapSkill` / `DRIVE TOTAL`；抽帧 `_frames_p0_cont31`
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。
## 本轮（2026-09-12 G2: ND VICTORY + The Hanged banner）

工程（≠ 验收）：
- Hard/ND 胜闪：**`VICTORY`** 主字（Robin r73）；普通仍 `勝利`+`victory`
- 骷髅头顶横幅：**`The Hanged`**（P0 t70）
- Inventory：`CHILD HP TOTAL` / `SKILL TIME` / `Mona's Tips`；抽帧 `_frames_p0_cont30`；SHOWTIME `RANK 7` 已见（数学仍 UNKNOWN）
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。
## 本轮（2026-09-12 G2: portrait Game view + hit chain）

工程（≠ 验收）：
- 有图形 win 冒烟 **PASS**（`vs-smoke-timing-20260912g.txt`）。截图像素 **1080×1920**（竖屏）。条 `strip_20260912g`：SHOWTIME 44f/1.471s；QTE 75f；PHASE 60f；victory 44f
- `hit-chain: tap in→num=0.000` / `slide in→num=0.000`（同帧；GT 列仍 UNKNOWN）
- 与 GT 并排：`our_slice/strip_20260912g/overlay/`。身份能对上，几何对不上。**不是** T27（中位≤1%），**不是** T28
- 合同还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: SKIP chrome + tray/foe/SHOWTIME）

## 此前（2026-09-12 G2: SKIP chrome + tray/foe/SHOWTIME）

工程（≠ 验收）：
- 对话跳过顶栏：**`>>` / `SKIP`** 叠字 stub（P0 t120；无 VN 接线）
- 此前同轮：`60 MAX Name`；ND 敌称号；SHOWTIME→`DRIVE GAUGE`；Fever tip inventory
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。
## 本轮（2026-09-12 G2: tray MAX Name + foe titles + SHOWTIME gauge）

工程（≠ 验收）：
- 底栏肖像满级：**`60 MAX Name`**（Robin tray）
- ND 敌牌帧锁称号：`Lovey-Dovey` / `Oracle` / `Battle Kitty`
- SHOWTIME 期间底条标签翻 **`DRIVE GAUGE`**；平时仍 `SKILL GAUGE`
- Inventory：Fever tip `Activate Fever Time!` / 14s body；`SlideRankLine`；抽帧 `_frames_p0_cont29`
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。
## 本轮（2026-09-12 G2: our-slice 30fps cue strip）

工程（≠ 验收）：
- 有图形 win 冒烟 **PASS**（`vs-smoke-timing-20260912f.txt`）：`CueStripRecorder` 缩图 JPG。SHOWTIME 43f/1.485s **28.95fps**；QTE 75f **29.99**；PHASE 60f **29.86**；victory 45f **30.23**。有 >2 帧间隙
- 条在 `docs/reference/gl-shutdown-pve/our_slice/strip_20260912f/`（含 playback mp4）。**不是** GT 叠图，**不是** 输入→命中
- 合同还原分仍 **0**。未开 G3。未声称 M1。未关 T28

## 此前（2026-09-12 G2: portrait costume titles）

工程（≠ 验收）：
- 肖像内 costume：P0 t435 帧锁名表 **`Lv.n Eclipse/Dark Water/Ocean Storm/Fire Serpent`**（非完整 EN 表）
- 水滴怪牌去重 `Small` 前缀；此前 Weak Point DEF ↓ / Barrier / LEVEL UP stamina / TIP!
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。
## 本轮（2026-09-12 G2: Weak Point DEF + Barrier + LEVEL UP stamina）

工程（≠ 验收）：
- 场上 debuff float / chip：**`Weak Point DEF ↓`**（P0 t460）；`VfxShield` **`Barrier`**
- BuffFloat **不再抹掉 EN** 浮字；Inspect 效果词走 `StatusChipText`
- LEVEL UP：双行 stamina（白 refill + 橙 max+1）；CLEAR 后延迟已有（`ClearHoldSec`）
- Inventory：`TIP!`；抽帧 `_frames_p0_cont28`（t360 SHOWTIME / t391 victory / t445 Fever tip）
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。
## 本轮（2026-09-12 G2: CLEAR before LEVEL UP）

工程（≠ 验收）：
- P0 t396 `CLEAR!!` 与 t470 `LEVEL UP!` 拆开：揭板后 **2.50s** 再叠模态（工程间隔，不是教程 ~74s，≠ T28）
- 有图形 win 冒烟 **PASS**（`vs-smoke-timing-20260912e.txt`）：`09_victory` 已目视 `勝利`/`victory`/`Tap the screen.`；`09_result` 已目视 `CLEAR!!` + 三星 + LEVEL/EXP 带，**不再被 LEVEL UP 盖住**
- 探针 SHOWTIME **1.472** / PHASE **2.079** / victory **1.448**
- 合同还原分仍 **0**。未开 G3。未声称 M1。未关 T28

## 此前（2026-09-12 G2: stacked clock + bloop Small plate）

工程（≠ 验收）：
- 战斗钟对齐 P0 t250：**`mm:ss` 叠 `BATTLE TIME`**
- 水滴怪场牌：**`Small` / `N Name`**（`FoePlateLine`）；骷髅仍 `Lv. N`
- 此前：百分比叠标签；CLEAR 中星；tray；Recovery；Hard MY HP
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。
## 本轮（2026-09-12 G2: PHASE splash field hide）

工程（≠ 验收）：
- P0 t64–t66：换波溅射时**藏场上站立单位**；HUD `PHASE n/m` **停在上一阶段**；去掉斩板，只留大 Heart + `PHASE n` + 关卡名
- 有图形 win 冒烟 **PASS**（`vs-smoke-timing-20260912d.txt`）：`10_wave` 已目视为空场 + 顶栏 `PHASE 1/2` + 中心 `PHASE 2`；`09_victory` 为 `勝利`/`victory`。探针 SHOWTIME **1.471** / PHASE **2.007** / victory **1.440**
- `09_result` 仍是 LEVEL UP 盖住 CLEAR 板。敌 HP 0→100 切换帧仍 UNKNOWN
- 合同还原分仍 **0**。未开 G3。未声称 M1。未关 T28

## 此前（2026-09-12 G2: stacked N% over meter labels）

工程（≠ 验收）：
- 顶/底百分比对齐 P0 t250/t508：**`N%` 叠在标签上**（`ENEMY HP TOTAL` / `ENEMY DRIVE` / `SKILL GAUGE` / `SKILL HP TOTAL`；Hard 次条 `MY HP TOTAL`）
- `BattleCueCopy.PctOverLabel`；抽帧 `_frames_p0_cont27`
- 此前：CLEAR 中星；tray `60 Name`+MAX；Recovery；ally Hero 两行
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。
## 本轮（2026-09-12 G2: CLEAR mid-star + costume inventory）

工程（≠ 验收）：
- CLEAR 三星弧：P0 t396 **中星更大**；奖励头像 **E** 移到右上
- Costume inventory：`Eclipse` / `Ocean Storm` / `Fire Serpent` / `Dark Water`（无 EN 表，不接线）
- 此前：tray `60 Name`+MAX；Recovery；Hard MY HP TOTAL；ally Hero 两行
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。
## 本轮（2026-09-12 G2: tray 60 Name + MAX）

工程（≠ 验收）：
- 底栏肖像对齐 P0 t83：**`60 Name`** + 旁置 **`MAX`**（满级）；costume 内 `Lv.n Eclipse/Ocean Storm` 仍 inventory
- 敌场牌：**`Lv. N  Name`**（t83 句点）
- 此前同轮：Recovery / Hard MY HP TOTAL / ally Hero 两行 / DriveReady EN / FEVER TIME!!
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。
## 本轮（2026-09-12 G2: lose+semi resmoke）

工程（≠ 验收）：
- 失败+半自动有图形冒烟 **PASS**（`vs-smoke-lose-20260912.txt`）：`result=DEFEAT`，`semi=True`，defeat 探针 **1.353s**
- 截图 `09b_defeat.png` 为 RESULT 板（`DEFEAT` / `HOME` / `RETRY`）；`06i_semi.png` 顶栏 `SEMI AUTO`
- `AUDIT.md` 补了 09-09 过时句对照
- 合同还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: Recovery + Hard MY HP + ally plate）

工程（≠ 验收）：
- Heal chip/float/VFX 对齐 Robin t25：**`Recovery`**（`StatusChipText` / `VfxBuffFloat` / `VfxHeal`）
- Hard 顶栏次条：**`N% MY HP TOTAL`**（party%）；普通仍 **`ENEMY DRIVE`**
- 友方场牌：**`Hero` / `N Name`** 两行（Robin t57）；敌牌仍 `Lv N  Name`
- Drive ready 肖像：**EN-only** `DRIVE SKILL READY`；Fever 窗/横幅显示 **`FEVER TIME!!`**
- Inventory：`PortraitCostumeStub` / Eclipse / Dark Water；Pause `Leaving returns to Home`
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。
## 本轮（2026-09-12 G2: untruncated cue probe）

工程（≠ 验收）：
- 冒烟等叠层自然结束后再拍。第二次有图形 Editor：**PASS**（`vs-smoke-timing-20260912b.txt`）
- 探针：SHOWTIME **1.473s**；PHASE **2.009s**；QTE 板 **2.502 / 2.501**（第三发 Fever 切入被截）；胜利闪 **1.434s**
- `TO FEVER` 板 2.50s，长于 PERFECT 1.90s（P1 板比章久）
- `TIMING_DIFF.md` 已写 Δ。**不是 T28**（无 30fps 同机位条，无输入→命中）
- 合同还原分仍 **0**。未开 G3。未声称 M1

## 此前（2026-09-12 G2: our-slice cue probe）

工程（≠ 验收）：
- 修编译后重开有图形 Editor 冒烟：**PASS**（`vs-smoke-timing-20260912.txt`）。用户 LocalLow 未写档
- `CueTimingProbe`：SHOWTIME 自然持有 **1.473s**（对 GT 1.50s）。PHASE 未见闭合；QTE/胜利被冒烟 KillAll/`SkipLive` 截断
- SHOWTIME/PHASE 战斗钟 `HoldSim` 按 P0 `BATTLE TIME` 停表对齐（Full Auto 仍 UNKNOWN）
- `TIMING_DIFF.md` 补了我方列。**不是 T28**。合同还原分仍 **0**
- 未开 G3。未声称 M1。P2 仍 bot-gate

## 此前（2026-09-12 G2: foe Lv plate + RANK spacing + Settings EN）

工程（≠ 验收）：
- 敌名牌：**`Lv N  Name`**（P0 t70 Fresh Skull）
- SHOWTIME stub：**`RANK 1 LV — / —`**（空格对齐 Robin t15）
- Inventory：**`AUTO SKILL` / `AREA —/—` / `VICTORY`**
- Pause 倍速芯片名 EN；Settings：**`BATTLE` / `AUTO` / `SPEED` / `SETTINGS`**
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: ResultTitle EN + Wave Enemies/BOSS + CORE）

工程（≠ 验收）：
- `GameRoot.ResultTitle`：**`CLEAR!!` / `DEFEAT`**（烟测 lose 认 DEFEAT；兼容旧 `失败`）
- Wave 预览：**`Enemies` / `BOSS`**
- CLEAR 奖励头像星：P0 t396 **2★ / 3★**
- Boss 场下 pip：**`CORE`**（Robin ~t68）；inventory **`>> SKIP`**
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: N% SKILL HP TOTAL order + LEADER/SUMMON EN）

工程（≠ 验收）：
- 底栏队伍弧对齐 P0 t504/t508：**`N% SKILL HP TOTAL`**（数在前）
- Inventory：**`TEAM HP TOTAL` / `PARTY HP TOTAL`**
- VFX：**`LEADER` / `SUMMON` / `UNCAP` / `REST`**；WB **`WORLD BOSS` / `DAMAGE`**
- 抽帧 `_frames_p0_cont22`
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 本轮（2026-09-12 G2: TIMING_DIFF draft）

工程（≠ 验收）：
- P0 ffprobe：296×640 vp9，平均 **30.00 fps**（容器 `r_frame_rate=16000/1` 不可信）
- 30fps 密抽写入 `TIMING_DIFF.md`（**草稿，不是 T28**）：SHOWTIME first-on→off **1.50s±2f**；PHASE 2 **2.03s±2f**；胜利闪屏点按结束；Robin PERFECT **1.90s**，`13% TO FEVER` = 0→40 中间帧
- 工程常数：`VfxShowtime.Duration=1.47`；`WaveCueBoard` 换波 `2.00`（倒下仍 1.20）；`VfxJudge` Perfect `1.90` + 0→N ease-in
- **没有**我方同机位录屏。输入→命中未测。合同还原分仍 **0**
- 未开 G3。未声称 M1。P2 仍 bot-gate

| Token | Value |
|---|---|
| TARGET | `TARGET_FROZEN_GT_PARTIAL`（handoff P0+P1 已落 ROOT；P2 bot-gate；帧对齐进行中。**不是验收**） |
| CONTRACT | `CONTRACT_FROZEN` |
| MILESTONE | `M1 IN_PROGRESS`（**未验收**） |
| G0 | `PASSED_WITH_GT_OPEN` |
| G1 | `PASSED_CONTRACT_FROZEN` |
| G2 | `IN_PROGRESS_CORE_SLICE`（**未过**） |
| PlayMode | `PASSED_SLICE_SMOKE`（含 lose+半自动：`06i_semi` / `09b_defeat`。另有胜利切片。**不是 GT，不是 M1 验收**） |
| EditMode | `NOT_RUN`（工程 **0** 个 UTF；`dotnet test` **155** 绿，**不是** EditMode） |
| primary GT | `LOCAL_PRIMARY_PARTIAL`（ROOT 已有 handoff **P0+P1** mp4；P2 重抓中。见 `docs/reference/gl-shutdown-pve/FETCH_LOG.txt`。**仍不是 M1 验收**） |
| GL formulas | `PARTIAL_TIP_ONLY`（P0 tip：QTE Fever% + 14s 窗已工程对齐；伤害/击数仍 `UNKNOWN`；无 `GL_FINAL_VERIFIED`） |
| `GL_FINAL_VERIFIED` | **forbidden / not created** |
| fidelity / 还原分 | **合同 0**（T27/T28/公式未测）。身份层另计，见 `CUE_SCORECARD.md`。不得报 90% |
| 功能核 | 内部一致性切片可跑；玩家可点选敌人作单目标焦点（工程，非 GT）；**不是**原作回归 |

## 功能核 vs 还原分（本闸）

独立 QA（`QA_EXECUTOR.md`，`M1-G2-QA-EXECUTOR`）已确认：默认夹具绿；执行器会改盾/控制/毒；Fever 不分走 Tap 系数；无 GT。`M1-G2-RESMOKE` PlayMode 切片冒烟 **PASS**（日志 Drive/Fever）。**不重做**战斗公式，**不采信** 夹具绿或切片 PASS = M1 过。

| 面 | 状态 | 说明 |
|---|---|---|
| 功能核 | 夹具自洽 | 已知 opcode 施加盾/Stun/Freeze/嘲讽/毒；Fever 用 `FeverChannelAtkCoef`/`FeverChannelFlat`；`GL_UNKNOWN` 不结算伤；`M1-G2-RESMOKE` 默认 `dotnet test` 0 失败 / 126 |
| 还原分 | **合同 0** | T27 叠图 / T28 时序 / GL 公式都没测。身份 cue 见 `CUE_SCORECARD.md`。有片 ≠ 还原通过 |
| 功能 IMPLEMENTED（内部一致性，非还原） | T01 / T03 / T10 / T15 / T18 | 同 seed 哈希、独立 SlideCd、毒=行动/受击、通道不混、Slide 非均匀 RNG |
| 还原 IMPLEMENTED | **无** | 全部 T 还原列 BLOCKED |

## 本轮（2026-09-12 G2: elem pip EN + GOT loot）

工程（≠ 验收）：
- 伤字元素 pip：**`F/W/G/L/D`**（非 火水木）
- 掉落飘字：**`GOT`**；好感 tag：**`Bond`**
- 敌场 **`COOL  SLIDE`**；**`BLIND`**；**`MY HP TOTAL`** inventory
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: foe COOL+SLIDE + BLIND）

工程（≠ 验收）：
- 敌场下 tag：充能+冷却同时时 **`COOL  SLIDE`**（Robin ~t48）
- Robin chip：**`BLIND`**；inventory **`MY HP TOTAL`**
- `CUE_SCORECARD` 身份行 EN；抽帧 cont20/21
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: BLIND chip + MY HP TOTAL inventory）

工程（≠ 验收）：
- Robin ~t48 头像 chip：**`BLIND`**（buff id `blind`）
- Inventory：**`MY HP TOTAL`**（Robin ND 顶栏次条；普通 P0 仍 **`ENEMY DRIVE`**）
- 此前：中战 **`SKILL GAUGE`**、`vampirism`、`N COOL TIME`；`CUE_SCORECARD` 身份行改 EN
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: SKILL GAUGE + vampirism + COOL TIME）

工程（≠ 验收）：
- 底栏驱动条中战主标：**`N% SKILL GAUGE`**（P0 t388/t510）；tip/SHOWTIME 变体仍 **`DRIVE GAUGE`**
- 去掉发明词 `DRIVE GAUGE FULL`；满条仍 `100% SKILL GAUGE`
- 头像 chip / float：**`vampirism`**（P0 t510 小写）
- Cool/Drive 时间：**`N COOL TIME` / `N DRIVE TIME`** 单行
- Sleep bubble glyph：**`Z`**
- 抽帧 `_frames_p0_cont19`（t250/380/388/510）
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: TOTAL n DAMAGE field order）

工程（≠ 验收）：
- Tip/combo/DPS 伤字对齐 P0 t445：**`TOTAL n DAMAGE`**（非 `TOTAL DAMAGE n`）
- 抽帧 `_frames_p0_cont18`（t358/t360/t445）
- LEVEL UP stamina EN；CLEAR LEVEL/EXP/GOLD 分词；Hero Lv plate
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: LEVEL UP stamina EN + CLEAR band split）

工程（≠ 验收）：
- LEVEL UP 体力行对齐 P0 t470：**`Stamina has been refilled to maximum.`** / **`Max stamina increased by 1.`**；Confirm 仅 EN
- 场上己方名牌：**`Hero  Lv N  Name`**
- CLEAR 黄带：数值与 **`LEVEL` / `EXP` / `GOLD`** 分词；奖励头像 3★ stub
- 此前本会话：CRIT / Drive READY EN / status VFX EN / GOOD·DEFEAT·buff floats
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: DRIVE READY EN + status VFX EN）

工程（≠ 验收）：
- Drive ready 爆发 / 头像行：**`DRIVE SKILL READY`** 主标（CN 副行）
- 状态 VFX tag：**`Stun` / `Silence` / `Taunt` / `Bleed` / `Poison` / `Sleep` / `Cleanse` / `Immortal` / `Reflect`**
- `VfxRouter.OnBuff` 认 EN chip 词（Barrier/Stun/…）
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: CRIT / Heal / Regen / FxWord EN）

工程（≠ 验收）：
- 暴击飘字 / punch / NamePop：**`CRIT`**（`VfxCritical` / `VfxDamagePopup` / HUD）
- 治疗 / 再生 tag：**`Heal` / `Regen`**
- HUD `FxWord`：**`Barrier` / `Taunt` / `Stun` / `Poison` / `ATK ↑` / `Haste` / `Debuff Blast`**
- Cue sheet CONTROL_CHIP 对齐 EN chips
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: GOOD / DEFEAT / LOOT / buff floats EN）

工程（≠ 验收）：
- QTE 票：**`GOOD`**（原 好）
- 胜负 splash 失败：**`DEFEAT`**；结果板 **`RESULT` / `LOOT` / Materials**
- Drive cut / warn 接线 EN；Boss intro：**`BOSS` / `WAVE`**
- Buff float：**`Regen` / `Vampirism` / `ATK Stack` / `Debuff Blast`**
- Tip inventory：P0 t345 弧标 `SKILL HP TOTAL` = tip mechanic **Total HP**
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: DRIVE SELECT / WARNING / CTA EN）

工程（≠ 验收）：
- Drive 选择板：**`DRIVE SELECT`** + **`Tap a Child to use Drive Skill`**
- Drive：**`READY?` / `DRIVE`**；Tap/Slide ready：**`TAP READY` / `SLIDE READY`**
- 敌警告：**`WARNING!!` / `ENEMY DRIVE SKILL`**
- 开战 stamp：**`FIGHT!`**
- Pause：**`HOME` / `CONTINUE` / `Repeat`**；结果：**`HOME` / `NEXT` / `RETRY` / `DEFEAT`**
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: COMBO / TOTAL DAMAGE / PHASE splash）

工程（≠ 验收）：
- Fever combo：**`COMBO`** + **`TOTAL DAMAGE`**（tip field TOTAL n DAMAGE）
- DPS 板：**`TOTAL DAMAGE` / `DPS` / `HEAL`**
- 换波 splash：大 **`PHASE n`** + 关卡名上置 + **Heart crest**（P0 t65）；`WAVE START` / `WAVE n`
- 死亡章工程 EN：**`DOWN` / `KO`**（P0 仍未见击破字）
- Inventory：**`Active Skill Addition`**（Robin r56）
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: WeakPoint / SKILL HP / DRIVE GAUGE / ally Hero）

工程（≠ 验收）：
- 场上弱点击：**`WeakPoint`**（Robin t68）；CRIT / RES / Heal EN floats
- 底栏队伍弧：**`SKILL HP TOTAL`**（P0 t508/t512；PARTY/TEAM 同角色变体）
- 驱动条：**`N% DRIVE GAUGE`**（tip t388）
- Fever 条幅主标：**`FEVER TIME!!`**（CN 狂热时间 为副）
- Drive crush 主标：**`DRIVE CRUSH`**
- 场上己方名牌：**`Hero  Lv  Name`** + 绿 pip（Robin t68）
- Boss 标：**`BOSS`**
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: PHASE/BATTLE TIME/PAUSE + EN chips）

工程（≠ 验收）：
- 顶栏：**`PHASE n/m`**、**`mm:ss BATTLE TIME`**、**`|| PAUSE`** / **CONTINUE**、**ESCAPE**
- 波次 splash：**`PHASE n`**；`VfxPhaseBar` PHASE
- 状态票：Robin **`DEF ↑`** / **`Barrier`** / **`Vampirism`** 等 EN chip
- 敌场：**`COOL`**（SlideCd）↔ **`SLIDE`**（Charge）切换
- 敌血文案：**`ENEMY HP TOTAL`**（P0 t365）
- 底栏队伍血：**`PARTY HP TOTAL`**（P0 t504；tip 亦 TEAM）
- 倍速：**`>> Xn SPEED`**（字母 X，非 ×）
- QTE 章：**`PERFECT!` / `GREAT!` / `GOOD`** + **`DAMAGE 150%`**（Robin t55）
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: CLEAR/LEVEL UP + primary EN HUD chrome）

工程（≠ 验收）：
- CLEAR（P0 t396）：黄带左 **LEVEL / EXP / GOLD**，右账号 + **EXP —/—**；奖励头像 uncap★ + E；CTA **HOME** / **NEXT|RETRY**
- LEVEL UP 模态（P0 t470）：crest、`Level up!`、`— → —`、体力 stub、黄 **Confirm**
- 顶栏：**`>> ×N SPEED`** / **`> FULL AUTO`** / SEMI / MANUAL；**`N% ENEMY HP LEFT`** / **`N% ENEMY DRIVE`**
- 底栏：**`N% SKILL GAUGE`** / **`TEAM HP`** / **`FEVER n%`**；中场 / QTE **`n% TO FEVER`**
- 头像：驱动就绪 **CN+EN**；**COOL TIME** / **DRIVE TIME** 双行
- 数值仍 UNKNOWN（—）；未抄教程 6/73
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: CLEAR 后 LEVEL UP 模态）

工程（≠ 验收）：
- 胜利 CLEAR splash 揭板后播 **LEVEL UP!** 模态 stub（P0 ~t470）：`LEVEL UP!` / `等级提升` / `Level — → —` / 体力 stub / `确认`
- 数值仍 UNKNOWN（—）；角色 debug `onLevel` 仍用短 chip
- `BattleCueCopy.LevelUp*`；`ResultBoard.Reveal(playLevelUp:)`；`GameRoot.DrawResult` 胜利路径接线
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: 敌方 SLIDE 充能条）

工程（≠ 验收）：
- 场上敌：绿 **SLIDE** 充能条 + `SLIDE` 标签（Robin ~t48 foe SLIDE gauge）
- 敌 Charge>8% 才显标签；填充接 `UnitState.Charge`
- 场上 **`抵抗`**：`RES`/`resist` 不再被 BuffFloat 当非中文丢掉；Robin mid-fight inventory
- `LiveSkillAdditionEn` inventory 变体
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: 冷却/驱动双行 + 顶栏 crest）

工程（≠ 验收）：
- 头像冷却/驱动时间：大数字 + 下行 **`冷却`** / **`驱动时间`**（P0 t365 / Robin `N COOL TIME` / `N DRIVE TIME`）
- 顶栏关卡名上 crest（`UiSprites.Heart`；GT 心形标）
- `DriveSkillAdditionEn` 常量 inventory（Robin）
- P0 Heaven's Dust 帧见 **PHASE 2/3** — 切片仍 Wave0/1 两波；三波记 inventory
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: 顶栏阶段 n/m + 吸血 chip）

工程（≠ 验收）：
- 顶栏关卡名下 **`阶段 n/m`**（P0 t250/t510 `PHASE n/m`）；`StagePhaseCount` 读 Wave0/Wave1
- 状态票：`lifesteal`/`vamp*` → **`吸血`**（P0 t510 EN vampirism）
- 敌名牌下小标（skull 角色；Spark stub）
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: ||暂停 + 点按已满环 + 头像HP条）

工程（≠ 验收）：
- 场上暂停钮：**`|| 暂停`**（GT `|| PAUSE`）；暂停中仍 `继续`
- 点按就绪：readyTag **`点按已满`** + 外圈黄环脉冲（tip t352/t358 Skill ready / Slide tip）
- 头像下薄 **HP fill**（P0/Robin tray）
- 场上敌名牌：元素色点（GT 水滴/太阳角色）
- 帧：Slide tip「Touch the icon, and slide it up!」已 inventory（`19_primary_handoff_frames.md`）
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

## 此前（2026-09-12 G2: ENEMY DRIVE + portrait `N MAX` + CLEAR 黄带）

工程（≠ 验收）：
- 顶栏次级：**`N% 敌驱动`**（P0 ~t250 `ENEMY DRIVE`；填充=场上敌最大 Charge，与三点 pip 同源；≠ GL 终验）
- 底栏头像：满级 **`60 MAX`** / 未满 **`Lv.N`**
- CLEAR（P0 t396）：奖励头像 stub + 黄带 `— LEVEL` / `EXP —/—` / `— EXP` / `— GOLD`
- 场上敌名牌：左侧 **`BattleLevel`** 数字（默认 1；非 Save 等级）
- `dotnet test` **155**
- 还原分仍 **0**。未开 G3。未声称 M1。P2 仍 bot-gate。

详帧对齐：`gt_search/19_primary_handoff_frames.md`。更早 2026-09-12 chrome 轮次见该文件 + `BLOCKED.md` continue 日志。

## 归档（2026-09-10 M1-G2-CAPTURE-STATES）

Editor PlayMode Vertical Slice Smoke（有图形）。只开一个 Editor（最终 pid **44048**，`BatchMode: 0`）。`vs-smoke.request` 20:06:39。日志 `[VS-SMOKE]` **PASS**，含 `07_drive` / `08_fever` / `09_result`。截图 mtime 20:06:46–20:06:51。`save-isolated=True`（`client/Temp/vs-smoke-save/save.json`）。用户 LocalLow `save.json` 仍 17:37:27，未改档。Editor 已退出。`client/Temp` 结果原件随退出被清；正文从日志回填到 `captures/` 与归档。

**我方切片，不是 GT。** 未宣称 M1 验收。未发明 `GL_FINAL_VERIFIED`。未开 G3。G2 仍未过。还原分仍 0。

## 此前（2026-09-10 M1-G2-RESMOKE）

重跑 Editor PlayMode Vertical Slice Smoke（有图形，非 `-nographics`）。先礼貌退出旧 Editor pid 27792。只开一个 Editor（pid **13000**，`BatchMode: 0`）。启动成功 ≠ 通过。已读新结果：`client/Temp/vs-smoke.result.txt` **PASS**（mtime `2026-09-10 17:20:24`）。日志：`drive qte slot=0` → `drive perfect fever=60` → 第二次 QTE → `fever=0 active=True` → `result 胜利`。

截到：Home / 契灵 / 编队 / 详情 / 战斗「开战」（Drive≈0%）。**没截到** Drive QTE / Fever / 结算画面（冒烟只在开战闪屏拍一帧）。窗口补拍 0 张。副本在 `BASELINE_LOGS/` 与 `our_slice/`（**我方切片，不是 GT**）。

未宣称 M1 验收。未发明 `GL_FINAL_VERIFIED`。未开 G3。G2 仍未过。还原分仍 0。

## 对照（2026-09-10 M1-G2-SUPP-CUES）

从本地 `mJrT2conPCI.mp4` 抽 Ragna 补充帧到 `docs/reference/gl-shutdown-pve/supplementary/ragna_gl/`。cue 草稿见该目录 `CUES.md`。`GL_PVE_GROUND_TRUTH.json` 分层：`supplementary` ≠ `primary`。**primary 仍 `NEEDS_FETCH`。** 未改 Unity。未跑 yt-dlp。未宣称还原通过。Raid/WB 只记模式差。纪念版大厅不能当战斗坐标。`battle_refs` HUD 轮廓 `REPORTED` + 混区。

## 此前（2026-09-10 M1-G2-FIX-DRIVE-SMOKE）

当时 PlayMode 仍 FAILED（未重开 Editor）。代码侧已修：开战 Hold 不再被 Full Auto/2s 看门狗放行，五人切片序列能打出 Drive/QTE/Fever。已被 `M1-G2-RESMOKE` 重跑。未宣称 M1 验收。

## 此前（2026-09-10 M1-G2-PLAY-RECORD）

把 PlayMode 从「怕写 Library 所以 NOT_RUN」推进到**真实跑了一次**。未升级 Unity。未 reset 脏树。未发明 `GL_FINAL_VERIFIED`。未开 G3。未重跑 `dotnet test`。未宣称 M1 验收。115/116 绿仍不是还原。

**路径：** Editor PlayMode Vertical Slice Smoke（有图形）。Win64 包 `2026-08-28` 相对今日 `catalog.json` / `BattleSim.cs` 过旧，未当成本轮冒烟。启动前无第二个 Editor；玩家包 pid 38412 仍在，未抢。

命令：

```
D:\Unity\Hub\Editor\6000.3.23f1\Editor\Unity.exe -projectPath F:\天命之子\client -logFile F:\天命之子\DC_RECON_KIT\m1\G0\BASELINE_LOGS\editor-playmode-20260910.log
```

无 `-batchmode` / `-nographics`。pid 27792。`BatchMode: 0`。许可 OK。会写 `client/Library`（预期）。启动冲掉预写 `Temp/vs-smoke.request`；加载后再写，日志 `[VS-SMOKE] entering play mode`。

**已读结果（启动成功 ≠ 通过）：** `client/Temp/vs-smoke.result.txt` → `FAIL drive never fired`，`phase=Result`，`tap 0` / `slide 1` / `result 胜利`。无 Fever。无结算截图。

截图已目视，非黑帧（大厅翡翠兔 / 契灵 / 编队 / 详情 / 战斗开战且 Drive≈0%）。实战仍 `JP_LEGACY_EMPIRICAL`。

| 项 | 路径 |
|---|---|
| 原始结果 | `client/Temp/vs-smoke.result.txt` |
| 原始截图 | `client/captures/` |
| Editor 日志 | `DC_RECON_KIT/m1/G0/BASELINE_LOGS/editor-playmode-20260910.log` |
| 归档 + 说明 | `DC_RECON_KIT/m1/G0/BASELINE_LOGS/` 、 `docs/reference/gl-shutdown-pve/our_slice/`（**我方切片，不是 GT**） |

Editor 仍占用 `client/`（pid 27792 + 其子 AssetImportWorker）。不要再开第二个 Editor。

## 此前（2026-09-10 M1-G2-CATALOG-OP）

`CatalogJson.Serialize` 技能/效果写出 `op`（与 Load 对称）；roundtrip 测走 temp、不写回 `catalog.json`。默认 `dotnet test` 0 失败 / 116。未宣称验收。

## 此前（2026-09-10 M1-G2-SMOKE-GATE）

闸门收尾，未扩 G3 / 20 人 / 新模式。未 reset 脏树。未升级 Unity。未宣称 M1 验收。

1. **catalog `op`：** `client/Assets/Content/catalog.json` 是合成夹具，原先技能/效果无 `op`。`ReadSkill`/`ReadEffect` 空 opcode 会失败关闭。已给**已有**合成技能与效果补 opcode，与 `Catalog.cs` builtin 一致（`DamageMath.ChannelOpcode(type)` / `EffectOpcodes.ForKind(kind)`）。未改角色名、技能名、系数。`CatalogJson.Serialize` **仍不写** `op`（本轮未改序列化）。
2. **PlayMode：** 当时 `NOT_RUN`。已被 `M1-G2-PLAY-RECORD` 推进为 **FAILED**（见上）。
3. 验收缺口清单：`DC_RECON_KIT/m1/G0/M1_UNGATE.md`。**不写 G3 入口。**

## PlayMode / Vertical Slice Smoke（`M1-G2-PLAY-RECORD` 原始记录）

| 项 | 事实 |
|---|---|
| Unity.exe | `D:\Unity\Hub\Editor\6000.3.23f1\Editor\Unity.exe` |
| 活动 Editor | pid **27792** 占用 `client/`（`Library/EditorInstance.json`）；子进程为 AssetImportWorker，不是第二份工程 |
| 玩家包 | `Resonance.exe` pid 38412 = `client/Builds/Win64`（2026-08-28，过旧）；本轮未用 |
| `client/Library` | 已写（预期） |
| 命令 | 见上；无 `-batchmode` |
| 触发 | 工程加载后写 `client/Temp/vs-smoke.request` → `vs-smoke.running` |
| 本轮结果 | `client/Temp/vs-smoke.result.txt` **已读**：`FAIL drive never fired` |
| 本轮截图 | `client/captures/`（已目视，非黑帧）；副本在 `BASELINE_LOGS/` 与 `our_slice/` |
| 判定 | **`FAILED`**。启动成功 ≠ 通过。无 Fever / 结算图。不得伪视觉通过 |

旧文件 `client/Builds/Win64/Temp/vs-smoke.result.txt` 是既往玩家包产物，**不是**本轮 Editor PlayMode。

## 此前（2026-09-10 M1-G2-EXECUTOR）

已知 opcode 按目录 kind 结算（伤害通道 / 盾 / Stun·Freeze / 毒·流血 / 嘲讽）；Fever 走 fever 通道、不借 Tap `AtkCoef`/`FlatPower`；`ClockPolicy.json` / `EffectSchema.json` 与代码对齐。测量值仍 UNKNOWN。M1 未验收。

## 此前（2026-09-10 M1-G0-GT-LOCAL）

只读本地搜录像。**未改 Unity。未再对 YouTube 跑 yt-dlp。未逐帧。**

- `GL_PVE_GROUND_TRUTH.json` 增加 `local_candidates`：38 条本地命中，**0 条可用主 GT**
- 缺件：`LOCAL_PVE_MISSING.md`（把国际服停服前后期、普通 5 人 PVE mp4 放进 `docs/reference/gl-shutdown-pve/`）
- `docs/reference/gl-shutdown-pve/` 仍 **0 mp4**（仅 `README.md` + `FETCH_LOG.txt`）

远程拉取 **BLOCKED**（本机 YouTube 族 TLS EOF，已证实）。等待本地入库。

## 此前（2026-09-10 战斗核 / GT 重试）

战斗核 REPAIR（未 REPLACE，未 checkout 旧扁平文件）：

- `client/Assets/Scripts/Resonance.Battle/Core/DamageMath.cs` — `NotMeasuredCode`；Auto ≠ Tap 通道；`Resolve` 对 `GL_UNKNOWN` 返回未测；KR Tap 二次衰减仅对照
- `client/Assets/Scripts/Resonance.Battle/Core/EffectOpcodes.cs` — 已知 opcode 表；未知抛 `UnknownOpcodeException`
- `client/Assets/Scripts/Resonance.Battle/Core/BattleEventLog.cs` — 可哈希事件日志；`BattleHudState` 多域（非单枚举）
- `client/Assets/Scripts/Resonance.Battle/Core/BattleSim.cs` — SlideCd 独立时钟；毒=on_action/on_hit_taken（禁止每秒 DoT）；可变 `Allies` 容量；默认 `Profile=GL_UNKNOWN`；未知 opcode → `Failed`
- `client/Assets/Scripts/Resonance.Battle/Core/Definitions.cs` — `SkillDef.Opcode` / `EffectDef.Opcode`
- `tools/BattleSim.Tests/BattleSimTests.cs` — 去掉 `catalog.json` 写盘
- `tools/BattleSim.Tests/M1CoreSliceTests.cs` — SlideCd / Auto / opcode / 毒 / 编队 / 哈希 / 禁终式

UI / 表现（未动共享 scene/prefab/meta）：

- `client/Assets/Scripts/Resonance.App/Battle/BattleHud.cs` — 竖屏 HUD 多域状态；Tap/Slide 手势已分离；Slide 就绪跟 SlideCd；布局标 `NEEDS_REFERENCE`（禁止伪 T27）
- `client/Assets/Scripts/Resonance.App/Battle/ICharacterPresentation.cs`
- `client/Assets/Scripts/Resonance.App/Battle/CharacterPresentationAdapter.cs`
- `CombatFeel.cs` / `VfxRouter.cs` / `BattleFighter.cs` / `CharacterPresenter.cs` — cue 跟事件走；伤害仍在 `BattleSim` 结算，不绑动画结束；保留现有骨骼

GT：

- `docs/reference/gl-shutdown-pve/` 已建，**仍 0 mp4**（`M1-G0-GT-RETRY` 换通道仍失败）
- `DC_RECON_KIT/m1/G0/GL_PVE_GROUND_TRUTH.*` — 重试命令/错误已记；oembed 只确认标题；2019 英文 5 人仍 `cross_era_gl`；未逐帧

未改：Unity 版本、URP、`TARGET.json`、共享 scene/prefab/meta、角色立绘/具体技能文案。

## 测试原始输出（本轮，改 catalog 后必跑）

```
dotnet test tools/BattleSim.Tests/BattleSim.Tests.csproj --nologo --verbosity minimal

BattleSim.Tests -> F:\天命之子\tools\BattleSim.Tests\bin\Debug\net6.0\BattleSim.Tests.dll
F:\天命之子\tools\BattleSim.Tests\bin\Debug\net6.0\BattleSim.Tests.dll (.NETCoreApp,Version=v6.0)的测试运行
正在启动测试执行，请稍候...
总共 1 个测试文件与指定模式相匹配。

已通过! - 失败:     0，通过:   115，已跳过:     0，总计:   115，持续时间: 150 ms - BattleSim.Tests.dll (net6.0)
```

退出码：`0`。**115 绿是夹具自洽，不是原作回归，不能升格为 M1 验收。**

PlayMode：当时 `NOT_RUN`。已被 `M1-G2-PLAY-RECORD` 推进为 **FAILED**。

## BLOCKED

- **`docs/reference/gl-shutdown-pve/` 0 mp4。** 国际服停服前后期、普通 5 人 PVE 实机仍缺。说明见 `LOCAL_PVE_MISSING.md`。远程 YouTube 族 TLS EOF，**不再**本机 yt-dlp。
- primary GT 仍 `CANDIDATE_NEEDS_FETCH` / `BLOCKED`。无 GT 不得报 UI 坐标 pass，不得报 numeric fidelity pass。
- GL 公式通道：`UNKNOWN`。对照仅 JP/KR legacy。无 `GL_FINAL_VERIFIED`。
- PlayMode：切片冒烟 **`PASSED_SLICE_SMOKE`**（`M1-G2-RESMOKE` 已读 PASS）。**无** Drive/QTE/Fever/结算截图。EditMode 仍 `NOT_RUN`。证据见 `BASELINE_LOGS/` 与 `our_slice/`（我方切片，不是 GT）。无 GT 不得报还原通过。
- 毒触发帧距、SlideCd 秒数：GL=`UNKNOWN`（U014）。
- Catalog `Load`/`BuildBuiltin` 仍全局换表、无并行锁。
- 默认编队 5；无 20 人/前后排。

## 不得 90%

M1 未验收。G2 未过。无 GT 不得报还原分。PlayMode 本轮切片冒烟 **PASS**（有图形；结果文件已读）。不得把大厅立绘、「开战」图或「胜利」日志写成原作回归。无 Drive/QTE/Fever/结算截图。

验收还缺什么见 `M1_UNGATE.md`。**M1 未验收，不写下一阶段 G3 入口。**
