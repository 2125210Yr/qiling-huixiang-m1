# GOAL LOCK — 2026-09-11（主人确认钉死）

## 一句话
**现在不是 G1。G1 已过。当前唯一主线是 G2 / M1 核心战斗切片；M1 验收通过之前，不开 G3/M2。**

## 阶段对照（以仓库文档为准）

| 代号 | 里程碑名 | 当前状态 | 含义 |
|---|---|---|---|
| G0 | 基线审计与目标冻结 | `PASSED_WITH_GT_OPEN` | 已过；GT 仍可继续搜，但不回头重做 G0 |
| G1 | 规则与界面契约 | `PASSED_CONTRACT_FROZEN` | **已过**；契约已冻，不重做 G0/G1 |
| **G2** | **= M1 高还原核心战斗切片** | `IN_PROGRESS_CORE_SLICE`（**未过**） | **当前主线** |
| G3 | = M2 可玩成长循环 | 未开 | **M1 验收通过后才开** |
| G4 / G5 | M3 / M4 | 未开 | 更后面 |

注意命名：文档里 **G2 就是 M1 这一闸**；**G3 才是下一个大里程碑（M2）**。口语「开 goal2」若指「下一阶段」，应对齐 **G3/M2**，不是再开一个 G2。

## 当前该干什么 / 不该干什么

### 要做（M1 验收硬路径）
1. 继续推进 **G2 核心切片**（Tap/Slide/Drive/QTE/Fever、受控/死亡/换波/结算、入口返回、表现同步）。
2. 补齐 **primary GT**：国际服停服前后期、竖屏、普通 5 人 PVE 实机录像入库 `DC_RECON_KIT/docs/reference/gl-shutdown-pve/`（现 **P0+P1 已入库**，见 `FETCH_LOG.txt`；P2 待重抓；有片 ≠ 验收）。
3. 有 GT 后再做逐帧对照；无 GT 则还原分保持 **0**，不得报 fidelity / 90%。
4. 夹具绿、切片冒烟 PASS = 功能核自洽，**≠ M1 验收**。

### 不做
- 不重做 G0/G1，不另起框架
- 不把 KR Raid / WB / 单份 Ragna 当唯一真值（可作补充/模式差异）
- 不开 G3/M2（养成闭环）、不扩 20 人/新模式充数
- 不发明 `GL_FINAL_VERIFIED`；不把 `dotnet test` 绿或无 GT 的 HUD 当验收

## 闸门顺序（钉死）

```
G0✓ → G1✓ → [当前] G2/M1 做完并验收 → 才开 G3/M2 → …
```

1. **现在：** 只打 G2/M1  
2. **M1 验收通过：** 才允许开 G3（成长循环，文档称 M2）  
3. **验收前任何「看起来能玩」都不算过闸**

## 真值口径（来自已确认 ChatGPT 决策）
优先：国际服停服前后期普通 5 人 PVE 实机竖屏录像，覆盖充能/Tap/Slide/Drive/Drive QTE/Fever/受击/死亡/换波/结算。  
补充：GL Ragna 可作国际服补充；KR Raid / WB 只研究模式差异，不混入普通战斗基准。

## 协调者执行口令
续跑 Agent 时只认：继续 G2/M1；报告已完成/未完成对照本表；未验收不得宣称 M1 过；不得自行跳到 G3。
