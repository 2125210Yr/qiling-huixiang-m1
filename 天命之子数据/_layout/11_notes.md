# 11 纪念版 OCR → wiki 正式名

规则：精确匹配；唯一后缀/包含（较短≥2 且较短/较长≥0.35）；禁止 索尔→哈索尔（多 1 字前缀）；禁止抢走另一行已精确占有的 wiki 名（同一天子重复纪念屏可共用）。3★ 对 罕见+常见。2★/1★ 无词条：只做目录 OCR 系统错字（缘→绿、姐比→妲比、束的→束缚），不升格成 3★ 正式名。

CSV 保留已有 `name_ocr` 列（未新增列）；score≥100 更新 `name` / `wiki_name`。

## 计数

| 稀有度 | 改名 | 已是正式名/不改 | 未匹配（拒绝猜） | 纪念版行数 |
|---|---:|---:|---:|---:|
| 5星 | 61 | 215 | 6 | 282 |
| 4星 | 11 | 66 | 0 | 77 |
| 3星 | 23 | 76 | 0 | 99 |
| 2星 | 8 | 43 | 2 | 53 |
| 1星 | 5 | 42 | 3 | 50 |
| **合计** | **108** | **442** | **11** | **561** |

CSV：`name` 更新 **104** 行，`wiki_name` 更新 **66** 行（其它列未动）。`name_ocr` 列原本就有，未新增列。

拒绝：`索尔`↛`大力无双哈索尔`；`德拉`↛`祝福的史古基`；`黑炎的赫斯提亚`↛`白炎的赫斯提亚`；两个 `艾莉希恩` 不抢已占用的 `船长艾莉希恩`。

## 改名样例

- `M5-011` 大力无双哈索尔·光 → 大力无双哈索尔（exact, score 200）
- `M5-014` 狡猬的列那 → 狡猾的列那（edit-distance-1, score 110）
- `M5-015` 殿护的拉尼 → 严谨的拉尼（unique suffix 拉尼, score 102）
- `M5-016` 泽卡提 → 抵抗的泽卡提（unique suffix/contain, score 140）
- `M5-020` 石见 → 螭龙石见（unique suffix/contain, score 140）
- `M5-024` 反擎者格琳戴儿 → 反击者格琳戴儿（edit-distance-1, score 110）
- `M5-037` 艾雅 → 川流艾雅（unique suffix/contain, score 140）
- `M5-040` 塞列欧斯 → 贤明的塞列欧斯（unique suffix/contain, score 140）
- `M5-042` 希妮 → 水中的希妮（unique suffix/contain, score 140）
- `M5-044` 诺特斯 → 独自跑的诺特斯（unique suffix/contain, score 140）
- `M5-046` 都桂花 → 通灵师都桂花（unique suffix/contain, score 140）
- `M5-051` 急的波利亚斯 → 急驰的波利亚斯（edit-distance-1, score 110）
