# O3 首领与指挥代码检查点

首领／精英调度、暂停编排和原样重试已实现并通过核心验证。Unity UI 验证仍未执行成功，因此本检查点不宣称 O3 完整验收，也不宣称新版可玩包或普通流程录像完成。

## 实现与验证

- N7 首领统一控制预告、读条、群攻、两面具与半血阶段；N4 精英使用同一有界调度器。暂停、速度和演出停顿只通过模拟时钟影响它。首领死亡／工程错误取消后续行动。
- 暂停时每人一条指令，替换保序，清除后重排。恢复后逐条通过 Submit 验证，不补充能；单体技能绑定敌方槽位与实例代数，目标已失效时明确拒绝。完整拒绝原因进入队列反馈。
- 冻结输入加入 N4 精英配置，当前 contentHash 为 `5be148f01a9107e6c6d149cf95798e5e57d810832b071f0e237268398e7506f6`。不迁移旧检查点、不清档。
- 核心主套件 480 通过、0 失败、1 项既有跳过；已知会污染 Catalog 的历史测试单独运行 1/1 通过。原始结果为 `evidence/o3/core-o3-*.trx`。已知旧顺序污染及隔离依据见 O2 报告。
- 新增真实战斗重试测试另行通过：同一持久化首领开局仅 AttemptId 改变，闲置策略 55.0 秒全灭，合法操作重试 90.2 秒获胜。`evidence/o4/retry-comparison.trx` 同时记录此测试和 O4 固定场景对照。N6 之前的路径是明确标记的存档夹具；战斗中不改生命、充能、暴击或胜负。
- 三种合法首领策略分别获得实际胜利：基础清面具 95.70 秒、防护家族 38.03 秒、散射家族加 C01 55.83 秒。不同策略的集火顺序不同，不用这些数字冒充同策略公平对照。
- 八项 Unity UI 测试已写入，两个 Unity 启动均在许可证握手阶段失败，未进入编译或测试，未生成 XML。原始启动日志、结束状态及诊断见 `evidence/o3/encounter-ui-red*` 与 `unity-license-diagnosis.md`。文件名中的 red 表示原计划用途，不表示观察到 UI 断言失败。

## 未完成项

Unity 许可客户端接收握手后停滞；同机在此前出现内存不足与跨应用 WMI 查询异常，怀疑 WMI/COM 阻塞但没有线程栈确认。已核对并终止仅本任务启动的挂起 Unity 与许可进程。未更改许可证、服务或权限。恢复编辑器后仍须执行八项 UI 测试、整套 Unity 回归、构建和普通实玩录像。

O4 的实际遗物反馈、O5 原创回放与最终打包继续进行。本提交没有推送、发布或覆盖旧产物。
