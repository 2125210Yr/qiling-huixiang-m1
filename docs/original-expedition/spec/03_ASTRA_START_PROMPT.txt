接手现有 Unity 项目，执行附件《01_MVP_SPEC.md》的 ORIGINAL-EXPEDITION-MVP v0.1。
请先完整阅读主规格，再对照《02_ACCEPTANCE_MATRIX.json》；该 JSON 的 NOT_RUN 是待办，不是测试结果。

本次已知基线：
仓库 2125210Yr/qiling-huixiang-m1
分支 m1-gt6-review
提交 8e56ee7c562c55ea336e3402f8c8c85d8c0223f4
核对本地实际 HEAD、工作区、F:/天命之子 与 F:/Resonance/client 的既有映射。
已有更新就在它上面继续，不回退、不用旧 master 覆盖、不丢弃未提交修改。

本次用户已确认新的产品目标：
固定五人队、固定章节与首领、每趟临时肉鸽构筑、首领前检查点原样重试。
保留厚涂静态插画方向；不以 Live2D 或最终画风定稿作为前提。
本规格取代旧 G2-EX02-SHOWTIME-CASTER-FOCUS 的“下一批唯一目标”。
旧原作保真 M1/G3 闸门不再阻塞本原创 MVP，但不能追认旧 M1 验收通过。
先按第0节记录目标覆盖，保留原历史资料与工程安全要求。

执行范围严格限制为主规格中的一章、五场战斗、十二件临时强化、三个家族、一个双阶段首领、最小等价永久解锁。
先完成可玩闭环，再补组合、首领、反馈与稳定交付。按 O0—O5 的依赖推进，不先写完整通用引擎。
这是实际代码、测试、构建和可玩结果任务，不是再输出一份计划就结束。
不安排商业验证期限，不扩营销、账号、联网或支付。Jev留作后续可选增量，本版离线核心不依赖模型。

特别注意已经核对到的接入风险：
1. GameRoot.Update 的旧胜利分支会 ApplyVictory；新远征必须走隔离结算，不调用永久升级链。
2. GameRoot.Persist 的空catch不适用于新奖励事务；独立新profile，写成功才发布状态。
3. OverlaySkill/OverlayEffect接收对象引用；克隆内容，不修改Catalog共享对象或另一场战斗。
4. 首领需要真实准备/预警/执行阶段；禁止旧敌方随机技能路径与新调度双重出手。
5. 暂停输入恢复后统一Submit；关闭QTE/Fever时不能留下隐藏等待。
6. 遗物只用真实结算和有限因果链，不解析跳字，不把未实现的Reflect等枚举当可用机制。
7. 回放独立记录配置、遗物、首领和结束边界；预期输出只比较，不参与重建答案。
8. 成型构筑允许快杀与跳过部分首领循环，不为保演出暗加血或限伤。

使用实际可用的最大有效并行：远征/存档、规则/首领、UI/反馈、内容/平衡、独立QA可分工。
BattleSim、BattleReplay、GameRoot及共享Unity资源由主集成者最终落盘，避免互相覆盖。
不reset --hard，不批量删文件，不覆盖个人档案，不自行升级工具链、push或发布。

交付匹配版本的源码、原始测试结果、独立Win64包、完整普通远征录像及首领失败重试片段。
真实UI流程开战后不注入血量、充能、Drive或胜负；测试夹具与普通实玩明确区分。
所有新验收实际执行后填写状态，不用旧310/0/1或旧视频冒充新模式验证。
遇到局部阻塞报告具体复现，继续完成独立可行部分；不要要求用户先补齐全部角色和美术。
完成规定范围后停止扩张，报告实际玩到哪里、限制和最多三项后续建议，交用户验收。
