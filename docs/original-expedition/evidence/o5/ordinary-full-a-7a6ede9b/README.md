# A 家族普通五战连续实玩：7a6ede9b

2026-09-21 实际 Windows Player，生产版本 7a6ede9b，普通入口。A seed **13486312**，RunId `439afa6220f34ece9f8d1c6762bcfae2`。完整 A 远征五场全部胜利；随后返回据点、打开上趟回顾，并正常创建新 C。独立证据核验只读用户档案与原 tape，没有运行 GUI、注入状态或修改生产代码。

## 五份实际回放严格匹配

每份 `replays/` 原始 tape 均用现有 `tools/OriginalReplay.Verify/bin/Release/net6.0/OriginalReplay.Verify.dll` 验证，**MATCH / exit 0**；原始 CLI JSON 在 `reports/*.verify.json`。DLL SHA-256 为 `8dd4971f1ef08a421e436559386af1f653fadd5b9ed0610d622fd8703b6b3d1e`。

| 节点 | Tape id | endTick | 主动成功/拒绝 | 开局遗物 |
| --- | --- | ---: | --- | --- |
| N1 | f8823184c2da44d2b2f208c9ce087ce3 | 685 | 6/0 | A01 |
| N2-backstage | cf4aed6fee7940b5aa2b27e0fc7eda93 | 1320 | 2/0 | A01 A02 |
| N4 | a532095270a243dbb179e4ddc6e09169 | 1452 | 4/0 | A01 A02 A04 |
| N5 | 89e7096e93e7401ea58ac258a93f3768 | 1809 | 1/0 | A01 A02 A04 A03 |
| N7 | f8e6b477c8cf4bfe8b367bc04719dcf2 | 3331 | 16/0 | A01 A02 A04 A03 |

schema 2，format `original-expedition-replay-v2`，content `original-expedition-content-v0.1.1`，hash `69aaa51d7fa5077a5e1d30b4c5f2fd042b62f932f86d0dec59a037762880fdbd`。路线为 N0→N1→N2-backstage→N3→N4→N5→N6→N7，观众席未同时完成。N1 早期曾通过普通“返回节点/继续”重开检查点，这是导航核验，最终完成战斗仍为五场。

## 实际遗物贡献与队列

`reports/battle-and-queue-analysis.json` 保存各场命令、原始 UI 回放输入、衍生结算、护盾生成及最终遗物触发序号。序号是“最近一次触发的全局序号”，不能当作触发次数。

- A01：N1、N2 各有一次实际 1120 反击；护盾吸收积蓄能的画面与终局状态均可核对。
- A02：同一固定五人基础护盾由 N1 的 `[1144,1496,1232,1100,1188]` / 总计 6160，变为 N2 之后 `[1430,1870,1540,1375,1485]` / 总计 7700，逐目标精确增加 25%。这是原生护盾调整，不会另造一条 A02 伤害。
- A04：N4 已实际释放一次 4480 主震荡；N7 三个 root 各请求 6720，有效合计 **16677**，过量 3483。
- A03：N7 **两个触发 root、三次命中**，请求合计 10080，有效 **7357**，过量 2723。root 111 仅剩一名面具（有效637）；root 236 对两名第二代面具各造成3360；末次 A04 主震荡击杀首领后没有继续扩散。
- **N5 已持有完整 A 家族，但只有一次护盾主动，蓄能达到 3360 后未释放主动震荡；A03/A04 实际触发为零。** N5 有 A02 增盾和 A01 蓄能证据，不能宣称完整反击组合在 N5 已释放。完整组合释放画面及数字由 N7 提供。

首领真实末战结算：有效敌伤 **65720**（其中遗物 **24034**）、实际吸收 **20963**、队员生命伤害 **46567**、有效治疗 **25020**、过量治疗0、主动成功16/拒绝0。与胜利画面及上趟摘要一致；这些是末场统计，不冒充五战总和。

N1 同 tick **206** 的原始 `Inputs` 明确记录：加入砚盾(1)→炽羽(0)→同 slot 覆盖炽羽→引灯(4)→清除炽羽→重加炽羽→逐锋(3)→恢复。最终 `Commands` 只执行 **[1,4,0,3]** 各一次，与屏幕顺序“砚盾、引灯、炽羽、逐锋”一致。严格 MATCH 同时覆盖原始队列输入和执行结果；截图019与视频232秒可直接查看。资源不足、目标失效等其他拒绝边界不由本轮证明。

## 胜利清空与下一趟

三份完整 profile 仅保留在本地录制目录。`reports/profile-transition-analysis.json` 只导出核验所需字段与 hash；均通过结构校验及原 `IntegritySha256` 重算。

| 快照 | revision | 核验结果 |
| --- | ---: | --- |
| profile-new-a-run.json | 36 | 新 A 仅 A01、全员满生命、无旧 B 遗物或战斗检查点；B 上趟摘要保留 |
| profile-after-a-victory.json | 51 | ActiveRun=null；完成五场，A 摘要保留；sweep/清章记录不重复增加，新解锁标志false |
| profile-new-c-run.json | 52 | 新 RunId `a02aeb8c634c4ec2979f7fbbbd7026b4`、seed14556671；仅C01、全员满生命、无战斗检查点；A摘要精确hash不变，永久解锁/发现保留 |

新 C 的满生命证据是进入战斗前的快照与视频1199秒。视频1214秒已战斗约8秒，生命正常下降，画面仅C01且明确“A未持有”；不能误称这张战斗帧仍满生命。

## 连续录像、关键帧与标签修正

本地原件：`F:/天命之子/dist/original-expedition-v01/full-a-7a6ede9b-20260921-1510/ordinary-full-a.mp4`。

时长 **1216.266667 秒（20:16.266667）**，18244帧，756×1344，H.264，15 fps采集；8941974字节，SHA-256 **`8fbac4be1c7d4780a920e82b94efab00e4e47132e3b7236dabc02b6b22c037da`**。操作者通过 `q` 正常结束、exit0；独立证据 agent 全片解码 `ffmpeg -hide_banner -v error -i <原件> -f null -` 也 exit0，无解码错误。

20张媒体时间抽帧全部由本 agent 亲自 `view_image` 检查，确有各场战斗状态、生命、蓄能、首领阶段及界面变化；不是旧 GDI 冻结录像。`reports/video-analysis.json` 提供全部观察和 ffprobe 元数据。暂停、N6等待时画面静止属于正常过程。15fps仅为采集频率，不替代 O-023 的真实渲染频率测量。

| 媒体秒数 | 实际画面 |
| --- | --- |
| 25 / 125 | 原创据点 → 新A前厅满生命 |
| 232 / 271 | N1覆盖/清除/重加后的队列 → 完成N1后双向分岔 |
| 318 / 363 | N2实际吸收蓄能 → 工坊已持有A04 |
| 480 / 506 | N4主敌已倒、蓄能已消费 → 完成N4后待进N5 |
| 528 / 570 | N5四件A遗物；护盾后蓄能3360、仅1主动 |
| 640 / 883 | N6门前整备 → N7真实开场、五人生命恢复、蓄能0 |
| 1016 / 1043 | N7一面具存活1.5倍、蓄能3360 → A04/A03亮、蓄能0 |
| 1091 | 阶段2两面具复建、2倍群攻读条；A04/A03亮 |
| 1118 / 1130 / 1143 | 五战胜利 → 据点发现8/12 → 上趟摘要 |
| 1199 / 1214 | 新C满生命待进前厅 → C01独立战斗、无A状态 |

截图文件名和 `events.json` 的 label 是操作意图，不能单凭名称判定实际状态；原文件名/字节保留以便审计：

- `046-A03-A04-mature-combo-trigger.jpg` 实际是 **N6 首领门前整备**，没有 N5 组合触发。
- `047-A-boss-start.jpg` 仍是确认出战前的门前画面；真实开场看视频883秒。
- `037-A04-overload-actually-triggered.jpg` 确有 N4 A04 亮起；`063` / `068` 确有 N7 A04/A03 同时亮起，后者处于阶段2。

`screens/` 原样复制23张截图，`events.json` 原样复制78条操作。截图写入时间不冒充其内部画面的准确视频时间；上述时间索引来自实际视频抽帧。

## 本轮能补齐的验收证据

- O-003：已具备同一普通新 A 远征从据点选择核心到五战胜利的有效连续录像，以及五份严格匹配的实际 tape。
- O-015：A01–A04 普通获取并在同一远征实际触发的证据齐备；不代表其余家族已全部完成。
- O-024：同 slot 覆盖、清除重加、多 slot顺序及恢复统一执行已有普通UI与实际回放输入证明。
- O-029：已有上一趟胜利清空→创建新趟→上趟摘要与永久解锁保留、临时构筑清空的实际连续证据；本轮 C 仅启动，不声称 C 已通关。
- O-036：完整 A 构筑在N5已持有，A01/A02实际作用；A03/A04完整释放在N7实际发生。N5完整释放不得由误标签代替。
- O-017/O-032/O-037：补充五场严格回放、实际贡献与队列/胜利/据点生命周期画面。
- O-038：补齐此生产版本完整流程录像部分；“新ZIP解压后脱离源码离线运行”及交付包指纹仍需相应包验证，不能由本视频单独推定。

MP4及完整个人profile不在本Git证据目录；`.gitattributes` 设置 `* -text`，`sha256-manifest.json` 记录全部归档文件及本地原件 hash。本目录未由证据 agent 提交或修改全局验收状态。
