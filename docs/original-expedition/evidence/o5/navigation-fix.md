# 普通试玩发现的节点导航修复

2026-09-21，基线 `e04e49e9`。普通无参数 Player 完成前厅、选择 B02 并进入观众席节点后，发现当前可执行选项位于完整路线与全部已持有强化之后。每次渲染回到页首，继续按钮需要反复滚轮才能找到。该次录像保留为 `dist/original-expedition-v01/play-e04e49e9-20260921/partial-navigation-issue-e04e49e9.mp4`，仅完成一场战斗，不作完整流程通过证据。

修复将当前选项放在队伍信息之后、路线与已持有强化之前；右侧增加独立滚动条，保留 12 像素设计间隙，不覆盖内容。滚动条仅在内容超出视口时显示。未修改战斗、随机数、存档或强化数值。

两项真实 Unity 布局回归在修改前均失败（`navigation-red-20260921.xml`），修改后完整 Editor 套件 84/84 通过、0 跳过（`navigation-green-20260921.xml`）。测试覆盖长页面首屏按钮可点击、选择顺序、滚动条绑定及从控件滚至末件强化。普通 Player 中的新版操作仍须在重新构建后核验。
