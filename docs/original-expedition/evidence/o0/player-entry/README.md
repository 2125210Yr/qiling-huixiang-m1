# O0/O1 开发包普通入口验证

2026-09-21（Asia/Shanghai），运行 `F:/Resonance/client/Builds/Win64/Resonance.exe`，没有启动参数、环境存档覆盖或状态注入。构建记录见 `../dev-o1-build-run.json`，当时的 EXE / App / Battle DLL 身份见 `build-hashes.json`。

通过 computer-use 操作真实窗口：据点选择 A01 → 前厅出战 → 点选敌人 → 自然充能后使用主攻手技能 → 暂停及恢复 → 普攻继续并取得胜利 → 三选一领取 A03 → 到达 N2 → 明确结束本趟 → 返回据点 → 选择 B01 开始新趟。新趟五人满血、只持有 B01；之后通过界面结束并关闭程序。

八张截图按顺序保存。第一趟种子 28678078，重新开始的第二趟种子 28785890。玩家输入只走界面，没有修改 HP、充能、种子或胜负。`Player.log` 没有匹配 Exception / Error / Failed 的记录。

原个人 `save.json` 及 `.bak` 在操作前后 SHA256 均为 `624CC9C6882BB512A7EFFE419F3B47332747BE33A33726796DF611612E55C2A7`，对应核验文件位于上一级目录。

这是 O0/O1 的一次真实单战闭环，**不是完整远征录像，不是最终交付包或 O2/O3 功能证明**。该开发包仍有标题高度裁切问题（后续由 glyph-red 的三个失败用例复现并修复）；修复后的界面以 `../ui-o1-final` 测试和后续渲染为准。此时遗物仅有内容与获取路径，运行效果、首领调度和暂停队列待后续阶段接入。开发包仍包含历史 Resources 素材，不作为可分发版本。
