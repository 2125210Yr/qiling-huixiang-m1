# 天命之子资料目录

从下面六个分类目录进入。已确认可清理的旧导出集中在 **可回收**；其他资料保留。

| 分类目录 | 内容 |
|---|---|
| `01_游戏与启动` | 原作单机整合包、游戏启动入口、自制游戏发行包和服务端入口 |
| `02_原始素材与资料` | 截图图集、录制视频、原作参考资料、数据手册和解包源数据 |
| `03_模型与恢复` | 原始角色素材、正式恢复模型、恢复脚本与数据库、旧提取工作区 |
| `04_角色创作与项目` | Codex 项目、游戏工程、勒达、猫耳女仆、画风试作和 Blender 工程 |
| `05_工具与文档` | 制作工具、研究文档、原模型解码工具链和运行环境 |
| `06_历史记录` | 旧诊断记录、旧重建包和本次整理清单入口 |

## 可回收

`可回收` 中有 **2,796 个旧导出目录，95,360 个文件，约 7.41 GiB**：

- `重复的旧导出`：1,907 个目录，模型、贴图等有效载荷已逐文件核对 SHA-256，另有保留副本。旧模型配置的模型与贴图引用也已核对。
- `未生成模型的编译残留`：889 个目录，只有编译文件和日志，没有生成模型。

这些是实际移动过来的文件，不是指向保留资料的链接。清理它们会失去对应的旧编译输出和日志，不影响当前正式模型与待修队列。保留副本仍在模型库中；不要同时删除它们。

本次没有删除文件。需要释放空间时，可自行删除整个 `可回收` 文件夹；文件如果还在 Windows 回收站，空间尚未释放。之前列出的两个 `20260916` 旧修复目录在本次整理开始时已经不存在，没有重复计入。

另有 110 个旧导出目录未满足重复条件，继续保留；拆分图层、源数据包和历史原始数据也没有判为废弃。

## 旧路径为何仍然存在

游戏、恢复脚本和历史证据使用了固定路径；部分项目还在 Git 管理或运行中。因此：

- 原作整合包、截图图集、录制视频和旧提取工作区已实际移动到分类目录，原位置保留隐藏的目录链接。
- 旧提取工作区的 2,479 个角色目录实际集中到 `03_模型与恢复/提取与旧恢复工作区/角色工作资料`，原路径也保留隐藏链接；源数据和批处理工具分别集中在该工作区的对应子目录。
- Git 项目和运行中的项目内部位置保持不变，通过分类目录中的 Windows 快捷方式进入。旧根目录入口被隐藏，未复制一份项目。
- `_sandbox/offline-pack-1.1` 是实际游戏运行文件；`_sandbox/moc-original-proof` 是恢复工具链。它们不是可以一概丢弃的临时目录。
- `client` 原本就是指向 `F:\Resonance\client` 的链接，外部游戏工程没有移动。

显示隐藏文件时仍能看到旧入口，这是为保持兼容而保留的。它们不是另一份素材，不能按重复文件删除。

## 整理记录

完整计划、执行日志、可回收清单、全部移动路径及核验结果保存在：

`整理记录/20260919-120329/`

核验结果：原清单 882,179 个文件均可找到；移动的 613,379 个文件大小和修改时间未变。2,492 个兼容目录链接及 28 个分类快捷入口有效，2,011 套正式模型的 CMO3、MOC3 和解析文件路径全部可访问。关键旧运行库及暂停计划的 SHA-256 未变。没有重新运行模型恢复或启动游戏。

本次整理不恢复已暂停的模型修复任务。当前正式模型为 2,011 套，剩余待修为 469 套。
